# Built binaries / Go build output
/eden-press
/eden
*.exe
*.test
*.out

# WebAssembly build artifacts
*.wasm

# C-ABI (cgo) host build output (scripts/build-capi-host.sh)
/bind/capi/build/

# Native mobile build products (scripts/build-android.sh / build-ios.sh) --
# outputs of DART-02, never committed; CI/local builds regenerate them.
*.so
*.a
*.dylib
*.xcframework/

# Node tooling used only by the throwaway corpus generator (tools/corpus-gen)
/node_modules/
tools/corpus-gen/node_modules/

# Dart/Flutter build tooling (bind/dart -- DART-04): regenerated by
# `flutter pub get`, never committed.
**/.dart_tool/
**/pubspec.lock
**/.flutter-plugins
**/.flutter-plugins-dependencies
**/build/

# Coverage output
coverage.out
coverage.txt
*.cover
profile.cov

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# DevFlow ephemeral skill marker — must never be committed
.planning/.skill-active
