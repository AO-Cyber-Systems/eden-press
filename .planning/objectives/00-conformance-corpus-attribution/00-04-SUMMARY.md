# 00-04 Summary — CommonMark + GFM full spec sweep (CONF-04)

**Requirement:** CONF-04 · **Wave:** 3 · **Status:** Complete

> Note: Task 1 (spec vendoring) was generated by the wave-3 executor but left uncommitted when
> that agent stalled at commit time; the orchestrator reconciled `go.mod` (consolidated `tidy`),
> authored the missing Task 2 code (`embed.go` + `spec_test.go`), verified, and committed both.

## What was built

The full CommonMark + GFM specification test suites, vendored in-repo and swept through the
DOM-normalized diff harness with pass/fail tracked **per spec section** — the CONF-04 acceptance
gate that every future engine objective is validated against (not the 32-case parser spike).

- **Vendored suites** (generated via each project's official `test/spec_tests.py --dump-tests`,
  then `go:embed`-ed for a hermetic test — no fetch at test time):
  - `conformance/spec/commonmark/spec.json` — CommonMark tag **0.31.2**, **652** examples
  - `conformance/spec/gfm/spec.json` — cmark-gfm master @499789b493, **670** examples
  - `conformance/spec/gfm/extensions.json` — cmark-gfm extensions, **30** examples
  - Deliberately the live cmark-gfm split (`spec.txt` + `extensions.txt`), NOT the frozen 2019
    `0.29.0.gfm.13` snapshot.
- **`conformance/spec/embed.go`** — `//go:embed` of the three suites as `spec.CommonMark`/`GFM`/`GFMExtensions`.
- **`conformance/runner/spec_test.go`** — `TestSpecSweep`: renders every example through the
  **shared** `runner.NewGoldmark()` / `NewGoldmarkGFM()` (from 00-02 — no local engine builder),
  DOM-diffs via `htmldiff.Equal`, records `report.Add(section, pass)` per section, applies an
  explicit `<s>`/`<del>` strikethrough allow-list (consulted-check + real-mismatch-not-excused
  check), and asserts structure (all examples counted, ≥20 sections, counts match VERSIONS.txt).
- **`VERSIONS.txt` / `README.md`** — pinned refs + measured counts + regeneration commands.
- **`NOTICE`** — CommonMark + cmark-gfm credited with exact tags/counts/licenses (per-PR NOTICE
  checklist dogfooded; edited only the spec-source region, leaving the Marp-corpus block for 00-05).

## key-files.created
- conformance/spec/embed.go
- conformance/runner/spec_test.go
- conformance/spec/commonmark/spec.json
- conformance/spec/gfm/spec.json
- conformance/spec/gfm/extensions.json
- conformance/spec/VERSIONS.txt

## Task Evidence
| Check | Command | Result |
|---|---|---|
| Build | `go build ./...` | exit 0 |
| Vet | `go vet ./conformance/...` | exit 0 |
| Spec sweep | `go test ./conformance/runner/ -run Spec -v` | PASS — **1258/1352 (93.0%)** goldmark baseline, 21 failing sections tracked per-section |
| Counts | `jq length` each suite | 652 / 670 / 30 (match VERSIONS.txt) |
| License | `addlicense -l mit -s -c "AO Cyber Systems" -check` | OK (both new .go files) |
| NOTICE | `grep -qi "commonmark" && grep -qi "cmark-gfm" NOTICE` | present with tags/counts |

## Deviations
- The engine-under-test in Objective 0 is goldmark itself (baseline); the gate is "the sweep runs
  and reports per section with the baseline recorded," NOT 100% green. The 93% baseline (94 failing,
  concentrated in raw-HTML/tab/loose-list sections) is recorded for future regression tracking.
- `go.mod` normalized `go 1.25`→`go 1.25.0` and 8 over-fetched `go.sum` lines pruned by the
  sanctioned consolidated `tidy` (safe here: sequential wave, no concurrent branches to prune).

## Self-Check: PASSED
All artifacts present on disk and committed (`f3a5d9a` data+NOTICE, `d5be409` runner); build/vet/test/addlicense all green; per-section reporting verified with a recorded baseline.
