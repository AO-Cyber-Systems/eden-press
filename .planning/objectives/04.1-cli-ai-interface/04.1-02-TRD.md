---
objective: 04.1-cli-ai-interface
trd: "02"
type: standard
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - cmd/eden-press/format.go
  - cmd/eden-press/agent_interface_test.go
  - scripts/check-no-chromedp.sh
  - AGENTS.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "`eden-press deck.md -o out.pptx --format pptx` writes a VALID .pptx: a ZIP whose OPC parts include `[Content_Types].xml` and `ppt/presentation.xml`, produced by `pptx.ToPPTX(out.Model, pptx.Options{})` — stdlib OOXML, ZERO chromedp. Bytes go to `-o` (or stdout if no `-o`)."
    - "The pptx path imports `convert/pptx` and passes the ALREADY-TYPED `out.Model` straight into `ToPPTX`, so `cmd/eden-press` still never imports `chase/model` directly. `convert/pptx` does NOT match `check-cli-imports.sh`'s `(chase|profiles)|chromedp` pattern, so BOTH `check-cli-imports.sh` and `check-no-chromedp.sh` stay green — check-cli-imports.sh is NOT relaxed (the OBJECTIVE's deliverable-5 relaxation is unnecessary and is NOT performed)."
    - "`scripts/check-no-chromedp.sh` TREES now includes `./cmd/...`, making \"the CLI stays chromedp-free\" a CI-ENFORCED invariant. The transitive closure of `./cmd/...` (now including convert/pptx + chase/model, both chrome-free) contains zero chromedp and the gate exits 0."
    - "`AGENTS.md` at the repo root documents the agent interface: the three `--format` values (html default | json | pptx), the JSON envelope schema (spelling out that a code block's raw source AND a math block's raw TeX both live in `block.text`, with `language`/`display` alongside — NO `source`/`tex` keys), the exit-code table (0 ok / 1 render / 2 usage), the JSON error-envelope shape, and copy-paste examples for inspecting + exporting a deck with zero browser."
  artifacts:
    - cmd/eden-press/format.go
    - scripts/check-no-chromedp.sh
    - AGENTS.md
    - cmd/eden-press/agent_interface_test.go
  key_links:
    - "The `pptx` case slots into the SAME `emitFormat` switch 04.1-01 created — pptx reuses the exact resolveInput→buildOptions→press.Render→emitFormat chain, so adding an export format is ONE case + one writer (writePPTX), never a parallel pipeline. writePPTX reuses 04.1-01's `writeOutputBytes` for the -o/stdout split and `cliFail` for the error path."
    - "Adding `./cmd/...` to check-no-chromedp.sh (rather than relaxing check-cli-imports.sh) is the load-bearing invariant flip: it converts \"CLI is chrome-free\" from an unenforced intention into a mechanical gate, and it passes PRECISELY because the pptx wiring imports only the chrome-free convert/pptx (proven: `go list -deps ./cmd/... ./convert/pptx/... | grep chromedp` is empty)."
---

<objective>
Complete the Chrome-free agent interface: wire `--format pptx` to the stdlib-OOXML `convert/pptx.ToPPTX(out.Model, pptx.Options{})` exporter (zero chromedp), flip the "CLI is chrome-free" intention into a CI-enforced gate by adding `./cmd/...` to `check-no-chromedp.sh`, document the whole agent surface in a repo-root `AGENTS.md`, and prove the interface end-to-end with an integration test (json parses + has structure; pptx is a valid zip; a bad input under `--format json` emits the JSON error envelope + non-zero exit).

Purpose: this closes the Objective-4↔library wiring gap for the Chrome-free export surface — an AI agent can now export an editable .pptx AND the chromedp-free property of `cmd/` becomes a mechanical invariant, not a hope. The reconciliation vs OBJECTIVE.md: deliverable 5 ("relax check-cli-imports.sh") is UNNECESSARY — `convert/pptx` never matches that gate's grep — so check-cli-imports.sh is left UNCHANGED and the real gate change lands in check-no-chromedp.sh.
Output: the `pptx` case + `writePPTX` in format.go (importing convert/pptx), the extended check-no-chromedp.sh TREES, AGENTS.md, and an agent-interface integration test.
</objective>

<file_tree>
eden-press/
├── AGENTS.md                              ← CREATE  (agent interface docs: formats, JSON schema, exit codes, examples)
├── scripts/
│   └── check-no-chromedp.sh               ← MODIFY  (add "./cmd/..." to TREES)
└── cmd/eden-press/
    ├── format.go                          ← MODIFY  (pptx case in emitFormat + writePPTX; import convert/pptx)
    └── agent_interface_test.go            ← CREATE  (json structure + pptx valid-zip + bad-input→exit-1+envelope)
</file_tree>

<execution_context>
@~/.claude/devflow/workflows/execute-trd.md
@~/.claude/devflow/templates/summary.md
</execution_context>

<embedded_context>

<codebase_examples>
`convert/pptx` PUBLIC surface (VERIFIED — convert/pptx/pptx.go): stdlib OOXML, ZERO chromedp.
```go
package pptx
func ToPPTX(doc *model.Document, opts Options) ([]byte, error) // deterministic; nil doc => error
type Options struct { SlideSize SlideSize } // zero value => SlideSize16x9 (widescreen)
```
Call it with the already-typed `out.Model` — do NOT import chase/model in cmd/eden-press:
```go
// cmd/eden-press/format.go — new pptx wiring (reuses 04.1-01's writeOutputBytes + cliFail)
import "github.com/AO-Cyber-Systems/eden-press/convert/pptx"

func writePPTX(cmd *cobra.Command, out press.Output) error {
    b, err := pptx.ToPPTX(out.Model, pptx.Options{}) // out.Model is *model.Document, typed by press
    if err != nil { return cliFail(cmd, exitRender, fmt.Errorf("format pptx: %w", err)) }
    return writeOutputBytes(cmd, b)
}
```
Replace 04.1-01's placeholder `case "pptx"` in emitFormat with:
```go
    case "pptx":
        return writePPTX(cmd, out)
```
Import-boundary VERIFIED: `github.com/AO-Cyber-Systems/eden-press/convert/pptx` does NOT match check-cli-imports.sh's `AO-Cyber-Systems/eden-press/(chase|profiles)|chromedp` grep, so the gate stays green with the new import.

check-no-chromedp.sh change (the ONE load-bearing gate flip) — extend TREES:
```bash
# BEFORE:
TREES=("./press/..." "./chase/..." "./profiles/..." "./bind/...")
# AFTER (add ./cmd/... — the CLI must now be a CI-enforced chrome-free tree):
TREES=("./press/..." "./chase/..." "./profiles/..." "./bind/..." "./cmd/...")
```
VERIFIED it passes: `go list -deps ./cmd/... ./convert/pptx/... | grep -i chromedp` is EMPTY (cmd + convert/pptx closure is chromedp-free). Optionally update the script's echo/comment to mention cmd/. Do NOT change the grep logic. Do NOT touch check-cli-imports.sh.

pptx integration test — validate the zip via stdlib archive/zip (no external deps):
```go
// cmd/eden-press/agent_interface_test.go
data, _ := os.ReadFile(outPath)                 // written by --format pptx -o
zr, err := zip.NewReader(bytes.NewReader(data), int64(len(data)))
// assert err == nil; assert names include "[Content_Types].xml" AND "ppt/presentation.xml"
```
</codebase_examples>

<anti_patterns>
- Do NOT relax check-cli-imports.sh. `convert/pptx` is already allowed by its grep; editing it is unnecessary scope + risks weakening the CLI-04 boundary. Leave it byte-for-byte unchanged.
- Do NOT import chase/model in cmd/eden-press to name `*model.Document` for the ToPPTX call — pass `out.Model` (already that type via press) directly.
- Do NOT add a raster/PNG/PDF path or touch preview/watch/serve — scope is convert-mode `--format pptx` only (OBJECTIVE Non-goals).
- Do NOT change check-no-chromedp.sh's grep or the other TREES entries — ONLY append `./cmd/...`.
- Do NOT edit go.mod/go.sum — convert/pptx and archive/zip need no new deps.
- Do NOT write the pptx to stdout in examples/tests that then re-parse text — pptx is binary; use `-o out.pptx`.
</anti_patterns>

<error_recovery>
- If check-no-chromedp.sh FAILS after adding ./cmd/...: run `go list -deps ./cmd/... | grep -i chromedp` and `go mod why <pkg>` — something pulled a browser in. Given only convert/pptx was added (verified chrome-free), suspect an accidental import; revert it. This gate MUST end at 0.
- If check-cli-imports.sh fails: cmd/eden-press imported chase/ or profiles/ directly — the only new import should be `convert/pptx`; remove any chase/model import and pass out.Model through.
- If ToPPTX errors with "doc is nil": out.Model was nil — that only happens on an empty/failed render; the render error should already have surfaced earlier. Ensure writePPTX runs AFTER a successful press.Render.
- If the pptx zip check fails: ToPPTX returned truncated bytes or the file wasn't flushed — ensure writeOutputBytes wrote to -o (not stdout) and the test reads the same path.
</error_recovery>

</embedded_context>

<context>
@.planning/objectives/04.1-cli-ai-interface/OBJECTIVE.md
@.planning/objectives/04.1-cli-ai-interface/04.1-01-TRD.md
@cmd/eden-press/format.go
@convert/pptx/pptx.go
@scripts/check-no-chromedp.sh
@scripts/check-cli-imports.sh
</context>

<gotchas>
HARD constraints (every task):
- **NEVER port 8080**; use 8091 for any local verification (none needed here).
- Every new `.go` file carries the Eden MIT header (copy lines 1-21 from `cmd/eden-press/convert.go`); AGENTS.md is Markdown (no license header). `check-no-chromedp.sh` keeps its existing header.
- **Commit ONLY via** `node ~/.claude/devflow/bin/df-tools.cjs commit "<msg>" --files <paths>` — never raw `git commit`.
- **Do NOT edit go.mod / go.sum** — `git diff go.mod go.sum` MUST be empty.
- **cmd/eden-press's only NEW import is `convert/pptx`** — still never chase/, profiles/, chromedp directly. Both gates stay green.
- Depends on 04.1-01 (emitFormat seam, writeOutputBytes, cliFail, --format flag) merged FIRST — this TRD fills the pptx case in the SAME format.go.
- Objective 6 (convert/pptx) is COMPLETE — do NOT modify convert/pptx; only CALL ToPPTX.
</gotchas>

## Test list

Behavior cases, outermost (CLI invocation) inward, happy + failure:

1. **pptx valid zip (happy):** `eden-press <deck>.md -o <tmp>.pptx --format pptx` → the file exists, is non-empty, and `archive/zip` opens it; entry names include `[Content_Types].xml` AND `ppt/presentation.xml` (a real OOXML package).
2. **json end-to-end structure (happy):** `eden-press <deck-with-code+math>.md --format json` → stdout parses; keys html/css/model/comments/meta present; a code block is `{kind:"code",text:...,language:"go"}` and a math block `{kind:"math",text:...,display:true}`; outline non-empty. (Re-asserts 04.1-01's contract through the full binary path.)
3. **error envelope + exit code (failure):** running the BUILT binary via `os/exec` on a nonexistent input with `--format json` exits non-zero (code 1) and prints `{"error":{"code":1,...}}` to stderr. (This is the one case that needs a real process to observe os.Exit.)
4. **gate enforcement:** `bash scripts/check-no-chromedp.sh` now scans `./cmd/...` and exits 0; `bash scripts/check-cli-imports.sh` still exits 0 (unchanged).

<tasks>

<task type="auto">
  <name>Task 1: Wire --format pptx (writePPTX + emitFormat case) + flip check-no-chromedp.sh to cover ./cmd/... + integration test</name>
  <files>cmd/eden-press/format.go, scripts/check-no-chromedp.sh, cmd/eden-press/agent_interface_test.go</files>
  <action>
Wire the Chrome-free pptx export and make the chrome-free-CLI invariant mechanical:
- format.go (MODIFY): add `import ".../convert/pptx"`; add `writePPTX(cmd, out)` per codebase_examples (ToPPTX(out.Model, pptx.Options{}) → writeOutputBytes; errors via cliFail(exitRender)); replace emitFormat's placeholder `case "pptx"` with `return writePPTX(cmd, out)`.
- scripts/check-no-chromedp.sh (MODIFY): append `"./cmd/..."` to the TREES array (keep grep + other entries unchanged); optionally extend the echo/comment to mention cmd/. Do NOT touch check-cli-imports.sh.
- agent_interface_test.go (CREATE, MIT header): Test-list cases 1-2 in-process via the rootCmd path (resetCfg; newRootCmd; SetArgs). Case 1: write a hand-built deck to a temp file, run with `-o <tmp>.pptx --format pptx`, read the file, open with archive/zip, assert entries include `[Content_Types].xml` and `ppt/presentation.xml`. Case 2: run with `--format json` + SetOut(buf), json.Unmarshal, assert the envelope keys + a code block (kind/text/language) + a math block (kind/text/display) + outline (re-uses the inline deck).

# CRITICAL: pass out.Model (typed *model.Document by press) into ToPPTX — do NOT import chase/model.
# GOTCHA: pptx is binary — always -o to a file in tests; never assert on stdout text for pptx.
# PATTERN: reuse 04.1-01's writeOutputBytes + cliFail — writePPTX is a thin adapter over pptx.ToPPTX.
# VERIFIED: adding ./cmd/... keeps check-no-chromedp at 0 (cmd+convert/pptx closure is chromedp-free).

Hand-built fixture only (no generated/LLM test data): an inline Go string deck with front-matter, a heading, a fenced ```go block, and a `$$...$$` math block.
  </action>
  <verify>gofmt -l cmd/eden-press/format.go cmd/eden-press/agent_interface_test.go && go build ./... && go test ./cmd/eden-press/... && go vet ./... && bash scripts/check-no-chromedp.sh && bash scripts/check-cli-imports.sh && go test ./conformance/... && go test ./profiles/slides/ -run TestGrepGate && addlicense -l mit -s -c "AO Cyber Systems" -check cmd/eden-press/agent_interface_test.go && test -z "$(git diff go.mod go.sum)"</verify>
  <done>`--format pptx -o out.pptx` produces a valid OOXML zip via convert/pptx (zero chromedp); check-no-chromedp.sh now scans ./cmd/... and exits 0; check-cli-imports.sh unchanged + PASS; json structure re-proven through the binary; conformance + grep-gate green; go.mod/go.sum diff empty.</done>
  <recovery>If check-no-chromedp fails, `go list -deps ./cmd/... | grep chromedp` + `go mod why` the offender and remove the accidental import. If check-cli-imports fails, remove any chase/model import; the only new import is convert/pptx.</recovery>
</task>

<task type="auto">
  <name>Task 2: AGENTS.md — repo-root agent interface docs + exec exit-code integration case</name>
  <files>AGENTS.md, cmd/eden-press/agent_interface_test.go</files>
  <action>
Write `AGENTS.md` at the repo root (Markdown, no license header) documenting the Chrome-free agent interface so an AI agent can script eden-press without reading Go:
- **Formats:** `--format html` (default; standalone zero-JS document), `--format json` (full press.Output envelope for inspection), `--format pptx` (editable OOXML export; use `-o out.pptx`). Note `--format` works on the default/convert mode; `-o/--output` chooses file vs stdout (stdout default; use `-o` for pptx binary).
- **JSON envelope schema:** show the shape `{ "html": str, "css": str, "model": { "meta": {...}, "sections": [ { "id": int, "attrs": {...}, "notes": [str], "blocks": [ {"kind": "paragraph|list|code|math|heading", "text": str, "level": int, "language": str, "display": bool, "ordered": bool, "items": [...] } ] } ], "outline": [ {"sectionId": int, "level": int, "text": str, "slug": str} ] }, "comments": [str], "meta": {...} }`. SPELL OUT: a code block's raw source AND a math block's raw TeX both live in `block.text`; `language` accompanies code, `display` (true=$$, false=$) accompanies math — there are NO `source`/`tex` keys. Fields are `omitempty` (absent when unused).
- **Exit codes:** table — `0` success, `1` render/runtime error, `2` usage/flag error (bad flag or unknown `--format`).
- **Error envelope:** under `--format json`, failures print `{"error":{"code":<int>,"message":<str>}}` to stderr.
- **Copy-paste examples:** `eden-press deck.md --format json | jq '.model.outline'`; `eden-press deck.md -o slides.pptx --format pptx`; `eden-press deck.md --format json > out.json || echo "failed: exit $?"`. Emphasize: NO browser, NO Node — pure Go; and that the CLI is CI-verified chromedp-free (check-no-chromedp.sh covers ./cmd/...).

Then add Test-list case 3 to agent_interface_test.go: `go build` the binary to a temp path (or `go run`), exec it via os/exec on a nonexistent input with `--format json`, assert the process ExitCode()==1 and stderr contains a JSON object with `error.code==1` that parses.

# PATTERN: keep AGENTS.md skimmable — headings + fenced examples an agent can copy verbatim.
# GOTCHA: the exec test needs a real process to observe os.Exit — use exec.Command(go, "run", "./cmd/eden-press", "/no/such.md", "--format", "json") from the repo root (or build once to t.TempDir()).
  </action>
  <verify>go build ./... && go test ./cmd/eden-press/... -run 'TestAgent|TestExitCode' -v && go vet ./... && gofmt -l cmd/eden-press/agent_interface_test.go && bash scripts/check-no-chromedp.sh && bash scripts/check-cli-imports.sh && test -f AGENTS.md && grep -q 'block.text' AGENTS.md && grep -qE 'exit code|Exit code|Exit Codes|exit codes' AGENTS.md</verify>
  <done>AGENTS.md documents formats + JSON envelope schema (block.text carries raw source/TeX; no source/tex keys) + exit-code table + error envelope + copy-paste examples; the exec integration case proves a bad input under --format json exits 1 with the JSON error envelope on stderr; all standing gates green.</done>
  <recovery>If the exec test's ExitCode is 0, main.go's exit sink isn't mapping cliError (04.1-01) — verify errors.As(*cliError)→os.Exit(code). If stderr has no envelope, cliFail's `cfg.String("format")=="json"` branch didn't fire — confirm --format reached cfg.</recovery>
</task>

</tasks>

<validation_gates>
<gofmt>gofmt -l cmd/eden-press/</gofmt>
<build>go build ./...</build>
<vet>go vet ./...</vet>
<test>go test ./...</test>
<no_chromedp>bash scripts/check-no-chromedp.sh</no_chromedp>
<cli_imports>bash scripts/check-cli-imports.sh</cli_imports>
<conformance>go test ./conformance/...</conformance>
<grep_gate>go test ./profiles/slides/ -run TestGrepGate</grep_gate>
<no_new_deps>test -z "$(git diff go.mod go.sum)"</no_new_deps>
</validation_gates>

<verification>
- `go run ./cmd/eden-press <deck>.md -o /tmp/out.pptx --format pptx && python3 -c 'import zipfile;z=zipfile.ZipFile("/tmp/out.pptx");n=z.namelist();assert "[Content_Types].xml" in n and "ppt/presentation.xml" in n;print("pptx OK",len(n),"parts")'` (use scratchpad, not /tmp, if preferred).
- `go run ./cmd/eden-press /no/such.md --format json ; echo "exit=$?"` → JSON error envelope on stderr + `exit=1`.
- `bash scripts/check-no-chromedp.sh` prints it is scanning `./cmd/...` among the trees and PASSes (0 chromedp); `bash scripts/check-cli-imports.sh` PASS (unchanged).
- `go list -deps ./cmd/... | grep -i chromedp` → empty. `git diff scripts/check-cli-imports.sh` → empty (NOT relaxed). `git diff go.mod go.sum` → empty.
- `test -f AGENTS.md && grep -q block.text AGENTS.md` → present.
</verification>

<success_criteria>
- OBJECTIVE Success Criterion 2: `eden-press deck.md -o out.pptx --format pptx` produces a valid .pptx via convert/pptx with NO chromedp in cmd/eden-press's dependency closure.
- OBJECTIVE Success Criterion 3 (complete, with 04.1-01): documented exit codes + JSON error envelope proven via a real-process exec test.
- OBJECTIVE Success Criterion 4: AGENTS.md documents the interface; all standing gates green — including check-no-chromedp now covering `./cmd/...` (=0) and check-cli-imports left UNCHANGED + PASS.
</success_criteria>

<output>
After completion, create `.planning/objectives/04.1-cli-ai-interface/04.1-02-SUMMARY.md`.
</output>
