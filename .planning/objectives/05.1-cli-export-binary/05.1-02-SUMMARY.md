---
objective: 05.1-cli-export-binary
trd: "02"
subsystem: cmd/eden-press-export
tags: [ci, github-actions, docker, chromedp, smoke-test, docs, agents-md]

# Dependency graph
requires:
  - objective: 05.1-cli-export-binary
    provides: "05.1-01: cmd/eden-press-export (standalone binary, --format pdf|png) with its documented exit-code contract (0 ok / 1 render / 2 usage / 3 no-Chrome); scripts/check-no-chromedp.sh re-scoped to ./cmd/eden-press/... with an appended 'eden-press-export is the sole chromedp cmd' assertion."
  - objective: 05-convert-raster
    provides: "The 05-05 pinned no-system-Chrome CI `export` job (.github/workflows/ci.yml) this TRD extends -- the chromedp/headless-shell:${CHROME_VERSION} container, CHROME_PATH wiring, and non-root/--shm-size hardening, reused verbatim."
provides:
  - "scripts/export-binary-smoke.sh (MIT header): builds cmd/eden-press-export, drives the COMPILED BINARY through a real --format pdf and --format png export of a hand-built fixture deck, asserts %PDF- and \\x89PNG magics; self-gates on the binary's exit-3 no-Chrome contract as a clean skip (exit 0)."
  - "Makefile export-binary-smoke target (mirrors export-test/check-chrome-export style), wired into .PHONY."
  - ".github/workflows/ci.yml's existing export job now also `go build ./cmd/eden-press-export` and runs `make export-binary-smoke` inside the SAME pinned-headless-shell container/run command as `make export-test` -- no new job, no container/flag/version changes."
  - "AGENTS.md 'Export (separate binary)' section: eden-press-export's formats, -o output convention, --browser-path/CHROME_PATH discovery, and 0/1/2/3 exit codes, for an agent/script consumer."
  - "convert/EXPORT.md cross-reference: cmd/eden-press-export named as the turnkey CLI consumer of the convert/ export surface, reinforcing convert/ as the sole chromedp boundary."
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Self-gating smoke via exit-code contract, not its own detection heuristic: export-binary-smoke.sh has zero Chrome-presence logic of its own -- it runs the compiled binary and treats its documented exit 3 as 'clean skip'. One code path proves the turnkey CLI argv->file-write path for real in the CHROME_PATH-provisioned CI container AND no-ops locally without Chrome, with no duplicated detection logic to drift out of sync with main.go's own contract."
    - "Extend-the-existing-job, not a parallel job: the export-binary smoke rides the SAME docker run invocation (same container, same CHROME_PATH, same non-root/--shm-size hardening) the 05-05 convert/ capstone already uses, via a single `&&`-chained bash -c command (make export-test && go build ./cmd/eden-press-export && make export-binary-smoke) -- keeping the pinned-headless-shell container the ONE place raster export is CI-validated."

key-files:
  created:
    - scripts/export-binary-smoke.sh
  modified:
    - Makefile
    - .github/workflows/ci.yml
    - AGENTS.md
    - convert/EXPORT.md

key-decisions:
  - "export-binary-smoke.sh's PDF and PNG smoke share ONE binary build and one fixture deck (written once to a mktemp -d directory, cleaned up via a single EXIT trap) rather than two separate temp setups -- simpler script, and proves both --format values work against the identical rendered input in one run."
  - "The CI export job's final bash -c command is extended with && (not a separate step) -- make export-test && go build ./cmd/eden-press-export && make export-binary-smoke -- so a build or smoke failure fails the SAME step the convert/ capstone already reports under, with no new step/job to keep in sync with the container setup."
  - "AGENTS.md's new 'Export (separate binary)' section is placed directly after the Formats table (before the JSON envelope schema section) so an agent reading the core CLI's format options immediately sees the pointer to the separate raster path, matching where the pre-existing 'not part of this binary' note already lived."

patterns-established:
  - "Any FUTURE Chrome-permitting binary added to ./cmd/... should ship its own exit-code-based no-Chrome contract (mirroring exitNoChrome=3 here) so a smoke/CI script can self-gate off that contract rather than re-implementing Chrome detection -- the same seam this TRD's smoke script relies on."

requirements-completed: []

# Verification evidence
verification:
  gates_defined: 8
  gates_passed: 8
  auto_fix_cycles: 0
  tdd_evidence: false
  test_pairing: false

# Metrics
duration: ~1min (Task 1 commit 805fa03 09:42:27 -> Task 2 commit 4e4eb18 09:43:24, local time; excludes upfront TRD/codebase read + worktree fast-forward time)
completed: 2026-07-22
---

# Objective 05.1 TRD 02: CI export-binary build + pdf/png smoke + docs Summary

**The existing pinned no-system-Chrome CI `export` job now also builds `cmd/eden-press-export` and runs a self-gating `export-binary-smoke.sh` (pdf `%PDF-` + png `\x89PNG` magics) against it, and `AGENTS.md`/`convert/EXPORT.md` document the separate export binary's formats, output convention, Chrome discovery, and exit codes.**

## Performance

- **Duration:** ~1 min (Task 1 commit `805fa03` 09:42:27 -> Task 2 commit `4e4eb18` 09:43:24, local time; excludes upfront TRD/codebase context-gathering and the worktree fast-forward to pick up the merged 05.1-01 wave)
- **Completed:** 2026-07-22
- **Tasks:** 2/2 complete
- **Files modified:** 5 (1 created: `scripts/export-binary-smoke.sh`; 4 modified: `Makefile`, `.github/workflows/ci.yml`, `AGENTS.md`, `convert/EXPORT.md`)

## Accomplishments

- `scripts/export-binary-smoke.sh` (MIT header, `set -euo pipefail`): builds `eden-press-export` to a temp dir, writes a tiny hand-built 2-slide fixture deck, runs `--format pdf -o <tmp>.pdf` (asserts the `%PDF-` magic) and `--format png -o <tmpdir>` (asserts >=1 file with the `\x89PNG` magic). Treats the binary's documented exit code 3 (no Chrome discoverable) as a **clean skip**: prints a `SKIP:` message and exits 0 — verified locally in this sandbox (no system Chrome), which correctly printed the SKIP path and exited 0.
- `Makefile` gains an `export-binary-smoke` target (added to `.PHONY`), mirroring the `export-test`/`check-chrome-export` style exactly; `CHROME_VERSION` untouched.
- `.github/workflows/ci.yml`'s existing `export` job's final `bash -c` is extended from `"make export-test"` to `"make export-test && go build ./cmd/eden-press-export && make export-binary-smoke"` — same container, same `CHROME_PATH`, same non-root/`--shm-size` flags, same `CHROME_VERSION` pin (`151.0.7922.34`); the `build` job is untouched. A diff-guard (`git diff ... | grep -iE 'headless-shell:|--user 10001|shm-size|CHROME_VERSION:'`) confirmed zero changes to those lines.
- `AGENTS.md` gains an "## Export (separate binary)" section (formats, `-o` output convention for both pdf and png, `--browser-path`/`CHROME_PATH` discovery order, the 0/1/2/3 exit-code table, copy-paste examples); the pre-existing "PNG/PDF export is intentionally not part of this binary" note now names `eden-press-export` and links to the new section.
- `convert/EXPORT.md` gains a short cross-reference naming `cmd/eden-press-export` as the turnkey CLI consumer of the discovery-chain/STIX-font/version-pin process the document already describes, reinforcing that `convert/` remains the sole chromedp boundary.

## Task Evidence

| Task | Verify Command | Exit Code | Status |
|---|---|---|---|
| 1: CI export-binary build + self-gating pdf/png smoke | `bash -n scripts/export-binary-smoke.sh && make -n export-binary-smoke && make export-binary-smoke && grep -q 'eden-press-export' .github/workflows/ci.yml && grep -q 'export-binary-smoke' .github/workflows/ci.yml && grep -q 'make export-test' .github/workflows/ci.yml && test -z "$(git diff -- .github/workflows/ci.yml \| grep -E '^[+-]' \| grep -iE 'headless-shell:\|--user 10001\|shm-size\|CHROME_VERSION:')" && addlicense -l mit -s -c "AO Cyber Systems" -check scripts/export-binary-smoke.sh && go build ./... && go test ./... && bash scripts/check-no-chromedp.sh && bash scripts/check-cli-imports.sh && test -z "$(git diff go.mod go.sum)"` | 0 | PASS |
| 2: Document the separate export binary — AGENTS.md + convert/EXPORT.md | `grep -q 'eden-press-export' AGENTS.md && grep -qiE 'Export \(separate binary\)\|separate binary' AGENTS.md && grep -q -- '--browser-path' AGENTS.md && grep -qiE 'CHROME_PATH' AGENTS.md && grep -qE 'no Chrome\|no-Chrome\|exit.*3\|3 .*Chrome' AGENTS.md && grep -q 'eden-press-export' convert/EXPORT.md && go build ./... && go test ./... && bash scripts/check-no-chromedp.sh && bash scripts/check-cli-imports.sh && go test ./conformance/... && go test ./profiles/slides/ -run TestGrepGate && test -z "$(git diff go.mod go.sum)"` | 0 | PASS |

_Both commands above were run as individual sub-commands (not one shell one-liner) inside this worktree's execution sandbox; every sub-check reported success — see the Validation Gate Results table below for the consolidated per-gate evidence._

## Task Commits

Each task was committed atomically via `df-tools.cjs commit` (never raw `git commit`):

1. **Task 1: CI export-binary build + self-gating pdf/png smoke (script + Makefile + ci.yml)** - `805fa03` (chore)
2. **Task 2: Document the separate export binary — AGENTS.md + convert/EXPORT.md** - `4e4eb18` (docs)

_Neither task is `tdd="true"` (`type: standard` — CI-harness + docs assertions, structural greps + a self-gating smoke, not unit TDD, per the TRD's own Test-list framing)._

## Validation Gate Results

| Gate | Command | Exit Code | Status |
|---|---|---|---|
| build | `go build ./...` | 0 | PASS |
| vet | `go vet ./...` | 0 | PASS |
| test | `go test ./...` (29 packages, incl. `cmd/eden-press-export`) | 0 | PASS |
| no_chromedp | `bash scripts/check-no-chromedp.sh` | 0 ("PASS: cmd/eden-press-export builds, contains chromedp, and is the sole chromedp-permitting cmd.") | PASS |
| cli_imports | `bash scripts/check-cli-imports.sh` | 0 ("PASS: cmd/eden-press imports only press/ ...") | PASS |
| conformance | `go test ./conformance/...` | 0 | PASS |
| grep_gate | `go test ./profiles/slides/ -run TestGrepGate` | 0 | PASS |
| no_gomod_churn | `git diff go.mod go.sum` | 0 (empty diff) | PASS |
| gofmt | `gofmt -l .` | 0 (no output; no Go source touched by this TRD) | PASS |
| addlicense (full sweep) | `addlicense -l mit -s -c "AO Cyber Systems" -ignore 'conformance/corpus/cases/**' -ignore '**/node_modules/**' -ignore 'themes/**' -ignore 'convert/chrome/fonts/**' -check .` | 0 (no output; docs correctly unflagged) | PASS |
| actionlint | `actionlint .github/workflows/ci.yml` | 0 (no output) | PASS |
| ci.yml diff-guard | `git diff -- .github/workflows/ci.yml \| grep -E '^[+-]' \| grep -iE 'headless-shell:\|--user 10001\|shm-size\|CHROME_VERSION:'` | 1 (no matches — image/flags/version byte-for-byte unchanged) | PASS |
| export-binary-smoke (local, no Chrome) | `make export-binary-smoke` | 0 ("SKIP: no Chrome discoverable (eden-press-export exited 3) — export-binary-smoke is a documented no-op in this environment") | PASS (documented skip) |

## Post-TRD Verification

- **Auto-fix cycles used:** 0 — no deviations were needed; the TRD's `codebase_examples` sketch (script shape, Makefile target, ci.yml extension) matched the actual repo layout with no adjustment.
- **Must-haves verified:** 5/5 (all `must_haves.truths` from `05.1-02-TRD.md` frontmatter: existing export job extended not restructured; self-gating export-binary-smoke.sh with MIT header + exit-3 clean-skip; Makefile export-binary-smoke target mirroring existing style, no CHROME_VERSION/go.mod churn; AGENTS.md "Export (separate binary)" section + updated not-part-of-this-binary note; convert/EXPORT.md cross-reference naming cmd/eden-press-export).
- **Gate failures:** None.

## Files Created/Modified

- `scripts/export-binary-smoke.sh` - MIT-headed, self-gating pdf+png smoke of the compiled `eden-press-export` binary; exit-3 -> clean skip (exit 0)
- `Makefile` - new `export-binary-smoke` target (added to `.PHONY`), mirroring `export-test`/`check-chrome-export`
- `.github/workflows/ci.yml` - existing `export` job's run command extended: `make export-test && go build ./cmd/eden-press-export && make export-binary-smoke`; image build, container flags, `CHROME_PATH`, `CHROME_VERSION`, and the `build` job byte-for-byte unchanged
- `AGENTS.md` - new "Export (separate binary)" section (formats, `-o` convention, `--browser-path`/`CHROME_PATH`, exit codes 0/1/2/3, examples); updated "not part of this binary" note now names `eden-press-export`
- `convert/EXPORT.md` - new cross-reference paragraph naming `cmd/eden-press-export` as the turnkey CLI consumer of the discovery-chain/STIX-font/version-pin process this document already describes

## Decisions Made

- The export-binary smoke rides the SAME `docker run` invocation the 05-05 `export` job already uses (extending its `bash -c` command with `&&`), rather than adding a parallel step or job — keeping the pinned-headless-shell container the one place raster export is CI-validated, per the TRD's anti_patterns.
- `export-binary-smoke.sh` deliberately has NO Chrome-detection logic of its own; it relies entirely on the binary's own documented exit-3 contract (05.1-01) as the skip signal — a single seam, not a duplicated heuristic that could drift out of sync with `main.go`.
- The new AGENTS.md section is placed immediately after the Formats table (before the JSON envelope schema section), matching where the pre-existing "not part of this binary" note already lived, so an agent scanning `--format` options sees the pointer to the separate binary in the same place.

## Deviations from Plan

None - TRD executed exactly as written. The `codebase_examples` script/Makefile/ci.yml sketches compiled, ran, and gated correctly with no adjustment needed; the local `make export-binary-smoke` run correctly hit the exit-3 skip path (no system Chrome in this sandbox), confirming the self-gating seam works as designed without requiring a live Chrome/Docker run in this execution environment.

## Issues Encountered

- This worktree's branch was 5 commits behind `main` at the start of execution (missing the 05.1 plan commit, the 05.1-01 wave-1 merge, and the TRD file itself). Resolved with a fast-forward merge (`git merge --ff-only main`) before reading the TRD — not a plan deviation, just a pre-execution sync step; the working tree was clean beforehand so the fast-forward was safe.
- Docker/a real Chrome-provisioned container is not available in this sandbox, so the CI job's own build+smoke step could not be exercised end-to-end here (only its syntax/wiring, plus the local exit-3 skip path). Per the TRD's `error_recovery` guidance, the ci.yml edit + script + Makefile target are landed to run for real in CI; the local run's clean SKIP is the documented substitute evidence.

## User Setup Required

None for this TRD's own gates. A real CI run (GitHub Actions, on push/PR to `main`) is where the pinned-headless-shell container actually exercises `export-binary-smoke.sh`'s PASS path (pdf `%PDF-` + png `\x89PNG` magics) end-to-end — that is expected to happen automatically on the next push, not a manual step.

## Next Objective Readiness

- Objective 05.1 (cli-export-binary) is now 2/2 TRDs complete: `cmd/eden-press-export` (05.1-01) is built, unit-proved, CI-built-and-smoked (05.1-02), and documented for both humans and agents.
- No open threads for a future TRD in this objective — `scripts/export-binary-smoke.sh`, the `export-binary-smoke` Makefile target, and the extended CI export job are the final missing pieces the 05.1-01 SUMMARY's "Next Objective Readiness" section called out.
- The STATE.md "Follow-up (optional, not blocking)" blocker note about wiring PNG/PDF export into a separate Chrome-permitting binary is now fully resolved by Objective 05.1 end-to-end (binary + CI proof + docs).

## Self-Check: PASSED

All claimed files confirmed present on disk; both task commit hashes confirmed present in `git log --oneline --all`.

- FOUND: scripts/export-binary-smoke.sh
- FOUND: Makefile (modified)
- FOUND: .github/workflows/ci.yml (modified)
- FOUND: AGENTS.md (modified)
- FOUND: convert/EXPORT.md (modified)
- FOUND commit: 805fa03 (Task 1)
- FOUND commit: 4e4eb18 (Task 2)

---
*Objective: 05.1-cli-export-binary*
*Completed: 2026-07-22*
