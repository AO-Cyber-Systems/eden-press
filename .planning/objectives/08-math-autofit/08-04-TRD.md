---
objective: 08-math-autofit
trd: "04"
type: tdd
wave: 4
depends_on: ["08-03"]
files_modified:
  - press/math/detect.go
  - press/math/detect_test.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "detect.go's `fallbackRE` is finalized to the STRUCTURAL CEILING ONLY — the constructs Chromium MathML-Core structurally CANNOT render (`\\tag`, `\\label`, and complex multi-column alignment env(s) confirmed against PROPOSAL §11: `alignat`/`array` retained pending the re-confirm, `aligned`/`align` and `cases` REMOVED because they now render natively). Over-broad entries are gone."
    - "`cases` is REMOVED from the trigger (PROPOSAL §11: KaTeX-quality as-shipped) and `aligned`/`align` are REMOVED (now fixed in 08-03) — a corpus/table test proves each of these now routes to NATIVE MathML, not the PNG fallback."
    - "The routing is corpus-tested, not manually inspected: a table in detect_test.go enumerates every construct → expected route (trigger set → PNG `<img class=\"math-fallback\">`; everything else, including the 8 fixed spike cases + cases + aligned, → `<math>`), and TestNeedsFallback's positive/negative lists are updated to the final rule."
    - "End-to-end routing holds through the real render path: `renderHTML(Option(\"\"), \"$$\\tag{1}…$$\")` produces the fallback `<img>`; the 8 fixed spike cases + `cases` + `aligned` all produce `<math>` and NO `<img>`."
    - "The PROPOSAL §11 8-case pass/fail table is RE-CONFIRMED (research Open Q3) as the first step, and `array`/`alignat`'s exact retained-vs-removed status is decided from that table and recorded in the SUMMARY."
  artifacts:
    - press/math/detect.go
    - press/math/detect_test.go
  key_links:
    - "depends_on [08-03] is a HARD semantic ordering, not a file conflict: removing `aligned`/`align` from the trigger is only safe AFTER 08-03's aligned→mtable fix lands, or the objective would regress currently-safe fallback into currently-broken native output (research Pitfall 1)."
    - "detect.go/detect_test.go are owned solely by this TRD — no other objective-8 TRD touches them, so despite the semantic dependency this is a clean single-file finalization."
---

<objective>
Finalize `press/math/detect.go`'s `fallbackRE` construct-detection predicate to route ONLY the permanent Chromium MathML-Core structural ceiling (`\tag`, `\label`, complex multi-column alignment) to the go-latex PNG fallback, and prove the routing with a corpus/table test rather than manual inspection (criterion 2). Because 08-02/08-03 fixed the converter bugs in-fork, the trigger set SHRINKS: `cases` (always rendered fine) and `aligned`/`align` (now fixed) are removed; everything except the true ceiling flows to native `<math>`. The PROPOSAL §11 re-confirm (research Open Q3) is task 1 so `array`/`alignat`'s retained-vs-removed status is decided from the authoritative table.

Purpose: RESOLVED DECISION #2 — the fallback trigger reflects a stable, spec-documented permanent gap (MathML Core has no `<mlabeledtr>`, drops most mtable presentational attributes), not a bug awaiting a fix. This is the LAST wave: it depends on the aligned fix having landed (08-03).
Output: finalized fallbackRE + updated TestNeedsFallback positives/negatives + a routing corpus/table test.
</objective>

<embedded_context>

<codebase_examples>
**Current BASELINE rule (press/math/detect.go) — to TIGHTEN:**
```go
var fallbackRE = regexp.MustCompile(`\\tag\b|\\label\b|\\begin\{(?:aligned|align|alignat|cases|array)\}`)
func needsFallback(rawLatex string) bool { return fallbackRE.MatchString(rawLatex) }
```
Remove `aligned` (fixed 08-03), `align` (fixed 08-03), `cases` (PROPOSAL §11: fine as-shipped). Keep `\tag`/`\label` (no `<mlabeledtr>` in MathML Core — permanent). Decide `alignat`/`array` from the PROPOSAL §11 re-confirm: `alignat` has extra `{n}`-column-count syntax latex2mathml may not model; `array` needs its exact §11 pass/fail row checked.

**Existing test to update (press/math/detect_test.go TestNeedsFallback):** positives = the constructs that MUST route to PNG; negatives = common math that MUST stay native. Move `aligned`/`align`/`cases` from positives to negatives; keep `\tag`/`\label` positive; place `alignat`/`array` per the §11 decision.

**End-to-end routing helper (press/math/math_test.go renderHTML):** `renderHTML(t, Option(""), src)` runs the full goldmark pipeline. `TestMathRender` already asserts `$x^2$`→`<math>` and heavy-aligned→`<img>`; UPDATE it (or add a sibling) so aligned now → `<math>` (not `<img>`) and a still-ceiling construct (`\tag`) → `<img>`.

**Routing corpus table (new, detect_test.go):** a table of {construct, expectFallback bool} covering: `\tag{1}`, `\label{e}`, `\begin{alignat}{2}…`, `\begin{array}{cc}…` (per decision) as TRUE; `\begin{cases}…`, `\begin{aligned}…`, `\begin{align}…`, and the 8 fixed spike cases as FALSE. Drive both `needsFallback(src)` and (for a subset) the end-to-end `renderHTML` route.
</codebase_examples>

<anti_patterns>
- Do NOT remove a construct from the trigger without its native-render fix having LANDED (research Pitfall 1). `aligned`/`align` removal is valid ONLY because 08-03 (a hard dependency) fixed them; `cases` removal is valid because it was never actually broken (§11).
- Do NOT widen the trigger back to a conservative "when in doubt, fall back" — the whole point is the trigger now means "permanent structural ceiling," a tight, testable set.
- Do NOT edit converter.go / mathml.go / go.mod — this TRD is detect.go + its test only. The converter is frozen after 08-03.
- Do NOT rely on manual/visual inspection for the routing claim — criterion 2 explicitly requires a corpus test. Every routing assertion is code.
- Do NOT drop the `\b` word-boundary on `\tag`/`\label` — `\tagged`/`\labelled` must not trip the trigger (the current rule's boundary logic is correct; preserve it).
</anti_patterns>

<error_recovery>
- If the PROPOSAL §11 re-confirm shows `array` renders at KaTeX-quality post-fix: remove it too (tighten further) and record it. If it shows `array`/`alignat` genuinely hit the ceiling (multi-column count syntax unmodeled): keep them and cite the §11 row.
- If a removed construct (aligned) STILL routes to PNG in the end-to-end test: detect.go was edited but the regex still matches it — re-check the alternation; the environment arm must no longer list aligned/align.
- If `cases` now emits broken native MathML (contradicting §11): STOP — 08-03's converter state regressed cases, or §11 was misread; do not remove cases from the trigger until it verifiably renders natively (Pitfall 1 warning-sign: a case that passes in isolation but the rule still routes it to PNG means the rule wasn't tightened; the inverse — routing native something that's broken — is worse).
</error_recovery>

</embedded_context>

<context>
@.planning/objectives/08-math-autofit/OBJECTIVE.md
@.planning/objectives/08-math-autofit/08-RESEARCH.md
@press/math/detect.go
@press/math/detect_test.go
@press/math/math_test.go
</context>

<gotchas>
HARD constraints (apply to every task):
- **NEVER port 8080.**
- **Commit ONLY via** `node ~/.claude/devflow/bin/df-tools.cjs commit …`.
- New/edited test + detect.go code carries the Eden MIT header (already present in both files).
- **Standing gates in `<verify>`:** gofmt, `go build ./...`, `go vet ./...`, `go test ./...`, `go test ./conformance/...`, `bash scripts/check-no-chromedp.sh`, `bash scripts/check-cli-imports.sh`, `go test ./profiles/slides/ -run TestGrepGate`, `addlicense … -ignore 'internal/latex2mathml/**' -check .`.
- TDD Iron Law: write the updated positive/negative + routing assertions FIRST (RED against the current over-broad rule), then tighten fallbackRE to GREEN.
- **This is wave 4 — depends on 08-03.** Do not start until the aligned→mtable fix is landed and green.
</gotchas>

## Test list

Write FIRST, drive RED → GREEN (hand-built inline LaTeX fixtures). Outermost (end-to-end route through renderHTML) → innermost (needsFallback predicate):

1. End-to-end: `renderHTML(Option(""), "$$\\begin{aligned}a&=b\\\\c&=d\\end{aligned}$$")` now contains `<math>` and NO `<img class="math-fallback">` (aligned routes native post-08-03).
2. End-to-end: `renderHTML(Option(""), "$$a=b\\tag{1}$$")` contains `<img class="math-fallback">` and NO `<math>` (tag hits the permanent ceiling).
3. `needsFallback` corpus table: `\tag{1}`, `\label{e}`, (+ `alignat`/`array` per §11 decision) → TRUE; `\begin{cases}…`, `\begin{aligned}…`, `\begin{align}…`, and the 8 fixed spike cases → FALSE.
4. `\tagged`/`\labelled` (prefix-sharing commands) → FALSE (word-boundary preserved).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-confirm PROPOSAL §11 (Open Q3) + write the finalized routing corpus test (RED)</name>
  <files>press/math/detect_test.go</files>
  <action>
FIRST, re-open PROPOSAL.md §11 (~lines 289-317) and re-confirm the exact 8-case pass/fail table — specifically the retained-vs-removed status of `array` and `alignat` (research Open Q3: `cases`=pass confirmed; `array`/`alignat` not independently re-verified). Decide the FINAL trigger set: `\tag`, `\label` always; `cases`/`aligned`/`align` removed; `array`/`alignat` kept ONLY if §11 (or a quick native-render check against the now-patched converter) shows them hitting the structural ceiling. Record the decision in SUMMARY.

THEN write the tests (RED against the current over-broad rule):
- Update TestNeedsFallback: move `aligned`/`align`/`cases` to the NEGATIVE list; keep `\tag`/`\label` positive; place `array`/`alignat` per the decision.
- Add a routing corpus table (Test-list case 3) driving `needsFallback` over {construct, expectFallback}.
- Add the word-boundary guard case (`\tagged`/`\labelled` → FALSE, Test-list case 4).

These fail now because the current regex still lists aligned/align/cases as triggers.

# CRITICAL: base the array/alignat decision on the AUTHORITATIVE §11 table, not assumption — this is the whole point of Open Q3.
  </action>
  <verify>go test ./press/math/... -run 'TestNeedsFallback|TestFallbackRouting' -v 2>&1 | tail -20  # expect RED (current rule too broad) && gofmt -l press/math/detect_test.go</verify>
  <done>The §11 table is re-confirmed and the array/alignat decision recorded; the updated positives/negatives + routing corpus table are written and correctly RED against the current over-broad fallbackRE.</done>
  <recovery>If §11 shows array renders fine post-fix, mark it a negative too; if it hits the ceiling, keep it positive and cite the row (error_recovery).</recovery>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten fallbackRE to the structural ceiling + prove end-to-end routing (GREEN)</name>
  <files>press/math/detect.go, press/math/detect_test.go</files>
  <action>
GREEN: rewrite `fallbackRE` in detect.go to the finalized structural-ceiling set from Task 1 — remove `aligned`, `align`, `cases`; keep `\tag\b`, `\label\b`; keep `array`/`alignat` only per the §11 decision. Update the detect.go doc comment to explain the trigger now means "permanent Chromium MathML-Core structural ceiling" (no `<mlabeledtr>`, dropped mtable presentational attributes), NOT "converter bugs" — cite that the converter bugs were fixed in 08-02/08-03.

Add the end-to-end routing assertions (Test-list cases 1-2) via renderHTML: aligned → `<math>` (no `<img>`); `\tag` → `<img class="math-fallback">` (no `<math>`). Update the existing TestMathRender if it still asserts aligned→fallback (that assertion is now inverted — aligned renders native).

# GOTCHA: TestMathRender (press/math/math_test.go) currently asserts heavy-aligned → fallback <img>. That is now WRONG — aligned renders native. Update it to route a still-ceiling construct (\tag) to the fallback instead, and assert aligned → <math>.
# PATTERN: keep needsFallback pure/allocation-free (it already is) — only the regex literal changes.
  </action>
  <verify>go test ./press/math/... -v && gofmt -l press/math/detect.go press/math/detect_test.go && go build ./... && go vet ./... && go test ./... && go test ./conformance/... && bash scripts/check-no-chromedp.sh && bash scripts/check-cli-imports.sh && go test ./profiles/slides/ -run TestGrepGate && addlicense -l mit -s -c "AO Cyber Systems" -ignore 'conformance/corpus/cases/**' -ignore '**/node_modules/**' -ignore 'themes/**' -ignore 'convert/chrome/fonts/**' -ignore 'internal/latex2mathml/**' -check .</verify>
  <done>fallbackRE triggers only on the structural ceiling; aligned/align/cases + the 8 spike cases route to native <math>; \tag/\label route to the PNG fallback; routing is corpus-tested end-to-end; all standing gates green.</done>
  <recovery>If aligned still routes to PNG, the regex alternation still lists it — remove aligned/align from the environment arm. If cases emits broken native MathML, STOP and do not remove it (Pitfall 1, error_recovery).</recovery>
</task>

</tasks>

<validation_gates>
<build>go build ./...</build>
<vet>go vet ./...</vet>
<test>go test ./press/math/... ./... && go test ./conformance/...</test>
<gates>bash scripts/check-no-chromedp.sh && bash scripts/check-cli-imports.sh && go test ./profiles/slides/ -run TestGrepGate</gates>
<license>addlicense -l mit -s -c "AO Cyber Systems" -ignore 'conformance/corpus/cases/**' -ignore '**/node_modules/**' -ignore 'themes/**' -ignore 'convert/chrome/fonts/**' -ignore 'internal/latex2mathml/**' -check .</license>
</validation_gates>

<verification>
- fallbackRE matches ONLY the finalized structural ceiling (`\tag`, `\label`, + `array`/`alignat` per the §11 decision); aligned/align/cases removed.
- The routing corpus table + updated TestNeedsFallback prove: trigger set → PNG fallback; the 8 fixed cases + cases + aligned → native `<math>` — all in code, no manual inspection.
- End-to-end: aligned renders `<math>` (no `<img>`); `\tag` renders the fallback `<img>`; TestMathRender updated accordingly.
- Standing gates all PASS.
</verification>

<success_criteria>
- The MathML fallback-trigger detector is a concrete, testable function routing exactly the permanent Chromium MathML-Core structural ceiling to the PNG path, covered by a corpus test (criterion 2).
- The trigger set is tightened to reflect the post-08-03 converter reality (cases/aligned/align render natively); PROPOSAL §11 re-confirmed.
</success_criteria>

<output>
After completion, create `.planning/objectives/08-math-autofit/08-04-SUMMARY.md`.
</output>
