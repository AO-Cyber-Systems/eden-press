---
objective: 08-math-autofit
trd: "06"
type: standard
wave: 3
depends_on: ["08-05"]
files_modified:
  - cmd/eden-press/flags.go
  - cmd/eden-press/htmldoc.go
  - cmd/eden-press/htmldoc_test.go
  - cmd/eden-press/convert_test.go
  - cmd/eden-press/preview.go
  - cmd/eden-press/serve.go
  - cmd/eden-press/watch.go
  - cmd/eden-press/format.go
  - press/browserjs.go
  - press/themecss_test.go
  - press/themes/themes.go
  - press/themes/themes_test.go
  - themes/embed.go
  - themes/browser-fit.js
  - NOTICE
  - AGENTS.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The `--auto-fit-script` flag is REMOVED (flags.go) and the `AutoFitScript` htmlDocOptions field + its `<script>press.BrowserFitJS()</script>` splice branch are REMOVED from assembleHTML (htmldoc.go), along with all four caller reads (preview.go, serve.go, watch.go, format.go) — plain HTML/PDF NEVER auto-fits and NEVER ships viewer-side auto-fit JS."
    - "The `<!--fit-->` / shrink MARKERS stay EMITTED by press/autofit.go, byte-for-byte unchanged (data-auto-scaling attribute + marp-fit-shrink wrapper) — they are inert on the web path but preserved for the Flutter consumer (08-07). press/autofit.go and press/autofit_test.go are NOT touched and stay green."
    - "The BrowserFitJS surface is fully removed: press/browserjs.go (the re-export) is deleted, themes.BrowserFitJS + its `//go:embed browser-fit.js` are removed from themes/embed.go, themes/browser-fit.js is DELETED, and press/themes/themes.go + the two tests that asserted the re-export (press/themecss_test.go TestBrowserFitJSReexport, press/themes/themes_test.go TestBrowserFitJS) are removed/updated — no browser-fit JS is embedded or reachable anywhere."
    - "A gate test/grep PROVES no viewer-side auto-fit JS ships in the default HTML path: assembleHTML(out, htmlDocOptions{}) contains ZERO `<script>` (the InjectScripts SSE-reload seam for watch/serve is UNAFFECTED), and a repo grep finds no `--auto-fit-script` / `browser-fit.js` / `BrowserFitJS` in production wiring."
    - "NOTICE drops the browser-fit.js verbatim-Marp entry (the file no longer ships) while preserving the Marp CSS-theme attribution (default/gaia/uncover.css remain); AGENTS.md's `--auto-fit-script` reference (line ~22) is updated to state plain HTML is zero-JS with no auto-fit and auto-fit is Flutter-only (08-07)."
  artifacts:
    - cmd/eden-press/htmldoc.go
    - cmd/eden-press/flags.go
    - themes/embed.go
    - NOTICE
    - AGENTS.md
  key_links:
    - "This removes the JS HALF of criterion 4 (no silent viewer-side JS auto-fit); 08-07 adds the Flutter TextPainter half. The two are independent — 08-07 does not depend on this, and this does not depend on 08-07 — but together they resolve the auto-fit decision gate."
    - "depends_on [08-05] is NOTICE-serialization only (both edit distinct NOTICE sections); functionally this TRD is independent of all math + font work. The InjectScripts reload seam (watch/serve) is deliberately preserved — only the AutoFitScript branch is removed."
---

<objective>
Resolve the auto-fit decision gate's web side (RESOLVED DECISION #2/#4): remove the `--auto-fit-script` opt-in flag, the `AutoFitScript` splice, and the entire vendored Marp browser-fit.js surface from the CLI/press so plain HTML/PDF output never ships viewer-side auto-fit JavaScript — while KEEPING the `<!--fit-->`/shrink marker emission (press/autofit.go) intact and inert on the web path for the Flutter consumer (08-07). Prove the removal with a gate asserting the default HTML carries zero `<script>`, and update NOTICE + AGENTS.md to reflect that auto-fit is now Flutter-only.

Purpose: research Pitfall 3 clarified the status quo was already opt-in-off-by-default JS, not silent JS — but the user's resolved decision is to DROP JS from the HTML path entirely and go Flutter-only. This TRD executes that removal cleanly, leaving the marker-emission battery (CORE-09) untouched so the markers still flow to the Dart layer.
Output: flag + splice + BrowserFitJS surface removed; browser-fit.js deleted; zero-JS gate; NOTICE + AGENTS.md updated.
</objective>

<file_tree>
cmd/eden-press/
├── flags.go              ← MODIFY  (remove the --auto-fit-script persistent flag)
├── htmldoc.go            ← MODIFY  (remove AutoFitScript field + the BrowserFitJS <script> splice branch)
├── htmldoc_test.go       ← MODIFY  (remove TestAssembleHTMLAutoFitScript; assert default = zero <script>)
├── convert_test.go       ← MODIFY  (remove TestRunConvertAutoFitScript)
├── preview.go            ← MODIFY  (remove the AutoFitScript: cfg.Bool(...) read)
├── serve.go              ← MODIFY  (remove the AutoFitScript read; KEEP the InjectScripts SSE-reload seam)
├── watch.go              ← MODIFY  (remove the AutoFitScript read; KEEP InjectScripts)
└── format.go             ← MODIFY  (remove the AutoFitScript read)
press/
├── browserjs.go          ← DELETE  (the press.BrowserFitJS re-export)
├── themecss_test.go      ← MODIFY  (remove TestBrowserFitJSReexport)
└── themes/
    ├── themes.go         ← MODIFY  (remove BrowserFitJS accessor if defined here)
    └── themes_test.go    ← MODIFY  (remove TestBrowserFitJS)
themes/
├── embed.go              ← MODIFY  (remove //go:embed browser-fit.js + BrowserFitJS)
└── browser-fit.js        ← DELETE  (the verbatim Marp JS auto-fit engine — no longer shipped)
NOTICE                     ← MODIFY  (drop the browser-fit.js entry; keep the Marp CSS-theme attribution)
AGENTS.md                  ← MODIFY  (update the --auto-fit-script reference → Flutter-only auto-fit)
</file_tree>

<execution_context>
@~/.claude/devflow/workflows/execute-trd.md
@~/.claude/devflow/templates/summary.md
</execution_context>

<embedded_context>

<codebase_examples>
**The splice to REMOVE (cmd/eden-press/htmldoc.go ~lines 36-42, 89-91):**
```go
type htmlDocOptions struct {
    AutoFitScript bool     // <-- REMOVE this field
    InjectScripts []string // <-- KEEP (watch/serve SSE reload seam — NOT auto-fit)
    Title         string
}
// in assembleHTML:
if opts.AutoFitScript {  // <-- REMOVE this whole branch
    b.WriteString("\n<script>\n" + press.BrowserFitJS() + "\n</script>\n")
}
for _, s := range opts.InjectScripts { … }  // <-- KEEP
```

**The flag to REMOVE (cmd/eden-press/flags.go:48):**
```go
f.Bool("auto-fit-script", false, "splice press.BrowserFitJS() into the assembled document (convert/watch/serve)")
```

**The four caller reads to REMOVE** (each one line):
- preview.go:86 `htmlDocOptions{AutoFitScript: cfg.Bool("auto-fit-script")}`
- format.go:140 `htmlDocOptions{AutoFitScript: cfg.Bool("auto-fit-script")}`
- serve.go:148 `AutoFitScript: cfg.Bool("auto-fit-script"),`
- watch.go:265 `AutoFitScript: cfg.Bool("auto-fit-script"),`
(serve/watch also set InjectScripts for the SSE reload client — leave that intact.)

**The BrowserFitJS surface to REMOVE:**
- press/browserjs.go — `func BrowserFitJS() string { return themes.BrowserFitJS() }` (whole file → delete)
- themes/embed.go:40-44 — `//go:embed browser-fit.js` + `func BrowserFitJS() string`
- themes/browser-fit.js — the verbatim Marp file (delete)
- Tests asserting the re-export: press/themecss_test.go TestBrowserFitJSReexport, press/themes/themes_test.go TestBrowserFitJS (remove).

**KEEP intact (do NOT touch) — press/autofit.go:** the CORE-09 marker emitter. `data-auto-scaling="fit"` + `marp-fit-shrink` markers stay emitted (inert on web, consumed by Flutter in 08-07). press/autofit_test.go stays green.

**AGENTS.md line ~22 (UPDATE):**
```
| `html` (default) | Standalone, zero-JS `<!doctype html>` document | `--auto-fit-script` splices one `<script>` for browser auto-fit; otherwise zero `<script>` tags |
```
→ rewrite: html output is always zero-JS (no auto-fit script); auto-fit is Flutter-only via the Dart binding (08-07); `<!--fit-->` markers are emitted but inert in HTML.

**NOTICE (~lines 20-31):** the Marp Core entry covers BOTH the .css themes AND browser-fit.js. Remove ONLY the browser-fit.js clause/lines; keep the default.css/gaia.css/uncover.css attribution (those still ship).
</codebase_examples>

<anti_patterns>
- Do NOT touch press/autofit.go or press/autofit_test.go — the `<!--fit-->` marker emission is preserved (inert on web, consumed by Flutter). Removing it would break 08-07's input.
- Do NOT remove the InjectScripts seam from htmlDocOptions / serve.go / watch.go — that carries the SSE live-reload client, which is a SEPARATE, legitimate script, not auto-fit. Only the AutoFitScript branch goes.
- Do NOT remove the Marp CSS-theme attribution from NOTICE — default/gaia/uncover.css still ship verbatim. Remove ONLY the browser-fit.js clause.
- Do NOT leave a dangling `press.BrowserFitJS` / `themes.BrowserFitJS` reference — deleting browser-fit.js while a caller still references the accessor breaks the build. Remove callers first (or in the same commit), then the accessor + embed + file.
- Do NOT introduce a CSS-only fluid-typography "replacement" for the removed JS — the decision is Flutter-only, and CSS `clamp()`/`cqw` is NOT behavior-equivalent to Marp's shrink-to-fit (research Pitfall 4). Plain HTML simply does not auto-fit now.
</anti_patterns>

<error_recovery>
- If `go build ./...` fails with "undefined: press.BrowserFitJS" after deleting browser-fit.js: a caller (htmldoc.go, or a test) still references it — grep `BrowserFitJS` across the repo and remove every production reference before deleting the accessor; tests referencing it are deleted in the same task.
- If a themes embed test fails because `//go:embed browser-fit.js` was removed but a `var` still references the embedded bytes: remove the var + accessor together; `go:embed` directives and their target vars are a unit.
- If the zero-<script> gate fails for watch/serve output: those legitimately inject the SSE reload script via InjectScripts — the gate must assert on the DEFAULT (htmlDocOptions{}) / convert path, not the live-reload modes. Scope the assertion to the no-InjectScripts case.
- If AGENTS.md or NOTICE still greps positive for `--auto-fit-script`/`browser-fit.js` after edits: a second occurrence was missed — grep and resolve all before the final gate.
</error_recovery>

</embedded_context>

<context>
@.planning/objectives/08-math-autofit/OBJECTIVE.md
@.planning/objectives/08-math-autofit/08-RESEARCH.md
@cmd/eden-press/htmldoc.go
@cmd/eden-press/flags.go
@press/browserjs.go
@press/autofit.go
@themes/embed.go
@NOTICE
@AGENTS.md
</context>

<gotchas>
HARD constraints (apply to every task):
- **NEVER port 8080.** serve's product default is 8321 (flags.go) — unchanged here.
- **Commit ONLY via** `node ~/.claude/devflow/bin/df-tools.cjs commit …`. Keep the build GREEN at every commit — remove callers and the accessor together so no dangling reference is committed.
- Deleted files (press/browserjs.go, themes/browser-fit.js) drop out of addlicense scope automatically; edited Go files keep their Eden MIT headers.
- **Standing gates in `<verify>`:** gofmt, `go build ./...`, `go vet ./...`, `go test ./...`, `go test ./conformance/...`, `bash scripts/check-no-chromedp.sh`, `bash scripts/check-cli-imports.sh`, `go test ./profiles/slides/ -run TestGrepGate`, `addlicense … -ignore 'internal/latex2mathml/**' -check .`.
- **NOTICE is shared with 08-01/08-05** — this TRD edits ONLY the Marp browser-fit.js clause; depends_on [08-05] serializes the writes.
</gotchas>

<tasks>

<task type="auto">
  <name>Task 1: Remove the --auto-fit-script flag + the AutoFitScript splice + all four caller reads (default HTML = zero JS)</name>
  <files>cmd/eden-press/flags.go, cmd/eden-press/htmldoc.go, cmd/eden-press/preview.go, cmd/eden-press/serve.go, cmd/eden-press/watch.go, cmd/eden-press/format.go, cmd/eden-press/htmldoc_test.go, cmd/eden-press/convert_test.go</files>
  <action>
Mechanical, atomic removal (keep the build green in one commit):
1. flags.go: delete the `f.Bool("auto-fit-script", …)` registration (line 48) and its doc comment block (~lines 32-36).
2. htmldoc.go: delete the `AutoFitScript bool` field (and its doc comment) from htmlDocOptions, and delete the `if opts.AutoFitScript { … press.BrowserFitJS() … }` branch in assembleHTML. KEEP the `InjectScripts` field + loop (SSE reload seam) and the `press` import IF still needed (press.Output type) — remove the import only if nothing else in the file uses it.
3. Remove the `AutoFitScript: cfg.Bool("auto-fit-script")` read from preview.go (86), format.go (140), serve.go (148), watch.go (265). In serve/watch, leave the InjectScripts assignment intact.
4. Tests: remove TestAssembleHTMLAutoFitScript (htmldoc_test.go) and TestRunConvertAutoFitScript (convert_test.go). ADD/keep an assertion that assembleHTML(out, htmlDocOptions{}) emits ZERO `<script>` (the zero-JS-default gate).

# CRITICAL: KEEP InjectScripts everywhere — it is the live-reload seam, not auto-fit.
# GOTCHA: press.BrowserFitJS still exists until Task 2 — this task just stops CALLING it; the build stays green because the accessor remains until Task 2 deletes it.
  </action>
  <verify>go build ./... && go vet ./... && go test ./cmd/eden-press/... -v && ! grep -rn "auto-fit-script\|AutoFitScript" cmd/eden-press/*.go && gofmt -l cmd/eden-press/*.go</verify>
  <done>The --auto-fit-script flag and every AutoFitScript reference are gone from cmd/eden-press; assembleHTML default output has zero <script>; InjectScripts reload seam intact; cmd tests green.</done>
  <recovery>If build breaks on a leftover AutoFitScript reference, grep cmd/eden-press for it and remove; if a serve/watch test breaks, ensure only AutoFitScript (not InjectScripts) was removed (error_recovery).</recovery>
</task>

<task type="auto">
  <name>Task 2: Delete the BrowserFitJS surface + browser-fit.js + add the no-viewer-JS gate + NOTICE</name>
  <files>press/browserjs.go, press/themecss_test.go, press/themes/themes.go, press/themes/themes_test.go, themes/embed.go, themes/browser-fit.js, NOTICE</files>
  <action>
Now that nothing calls it (Task 1), remove the entire browser-fit JS surface:
1. Delete press/browserjs.go (the `press.BrowserFitJS` re-export).
2. themes/embed.go: remove the `//go:embed browser-fit.js` directive, its `var`, and `func BrowserFitJS()`.
3. Delete themes/browser-fit.js.
4. press/themes/themes.go: remove the BrowserFitJS accessor if defined/re-exported there.
5. Remove the now-obsolete tests: press/themecss_test.go TestBrowserFitJSReexport; press/themes/themes_test.go TestBrowserFitJS.
6. NOTICE: remove ONLY the browser-fit.js clause from the Marp Core entry (keep the default/gaia/uncover.css attribution — those still ship).
7. Add the no-viewer-side-JS-auto-fit GATE: a Go test (in cmd/eden-press or press) asserting the default assembled HTML has zero `<script>`, plus a documented repo grep in `<verify>` proving no `BrowserFitJS`/`browser-fit.js`/`--auto-fit-script` remains in production wiring.

# CRITICAL: remove callers (Task 1) BEFORE the accessor (this task) — never commit a dangling BrowserFitJS reference.
# GOTCHA: `//go:embed` + its target var are a unit — remove both or the build fails (error_recovery).
# GOTCHA: keep the Marp CSS-theme NOTICE attribution — only the JS clause goes.
  </action>
  <verify>go build ./... && go vet ./... && go test ./... && go test ./conformance/... && ! grep -rn "BrowserFitJS\|browser-fit.js" --include=*.go . && test ! -f themes/browser-fit.js && test ! -f press/browserjs.go && ! grep -qi "browser-fit.js" NOTICE && bash scripts/check-no-chromedp.sh && bash scripts/check-cli-imports.sh && go test ./profiles/slides/ -run TestGrepGate && addlicense -l mit -s -c "AO Cyber Systems" -ignore 'conformance/corpus/cases/**' -ignore '**/node_modules/**' -ignore 'themes/**' -ignore 'convert/chrome/fonts/**' -ignore 'internal/latex2mathml/**' -check .</verify>
  <done>The entire BrowserFitJS surface + browser-fit.js are removed; no production/test code references them; NOTICE drops the JS clause (keeps CSS attribution); the zero-JS gate proves no viewer-side auto-fit JS ships; standing gates green.</done>
  <recovery>If build breaks on undefined BrowserFitJS, a caller survived Task 1 — grep and remove. If a themes embed var dangles, remove it with its go:embed directive (error_recovery).</recovery>
</task>

<task type="auto">
  <name>Task 3: Update AGENTS.md + docs; confirm marker emission preserved and inert on web</name>
  <files>AGENTS.md</files>
  <action>
Update AGENTS.md line ~22 (and any other `--auto-fit-script` mention): the `html` output row now reads that HTML is ALWAYS zero-JS (no auto-fit script exists); `<!--fit-->` markers are emitted into the DOM but inert on the web path; auto-fit is Flutter-only via the Dart binding (08-07). Remove the `--auto-fit-script` column text entirely.

Confirm (no code change) that press/autofit.go still emits the markers and press/autofit_test.go is green — the CORE-09 marker battery is deliberately preserved as 08-07's input.

# PATTERN: keep AGENTS.md's format table accurate for AI consumers — the flag no longer exists, so it must not appear as an option.
  </action>
  <verify>! grep -n "auto-fit-script" AGENTS.md && go test ./press/ -run 'TestAutofit|TestAutoFit' -v && grep -qi "flutter" AGENTS.md</verify>
  <done>AGENTS.md reflects zero-JS HTML + Flutter-only auto-fit; the `<!--fit-->` marker emission (press/autofit.go) is confirmed intact and its tests green.</done>
  <recovery>If a doc still references the removed flag, grep AGENTS.md/README/CONTRIBUTING for `auto-fit-script` and resolve all (error_recovery).</recovery>
</task>

</tasks>

<validation_gates>
<build>go build ./...</build>
<vet>go vet ./...</vet>
<test>go test ./... && go test ./conformance/...</test>
<gates>bash scripts/check-no-chromedp.sh && bash scripts/check-cli-imports.sh && go test ./profiles/slides/ -run TestGrepGate</gates>
<zero_js>go test ./cmd/eden-press/... (default assembleHTML = zero &lt;script&gt;) &amp;&amp; ! grep -rn "BrowserFitJS|browser-fit.js|auto-fit-script" --include=*.go .</zero_js>
<license>addlicense -l mit -s -c "AO Cyber Systems" -ignore 'conformance/corpus/cases/**' -ignore '**/node_modules/**' -ignore 'themes/**' -ignore 'convert/chrome/fonts/**' -ignore 'internal/latex2mathml/**' -check .</license>
</validation_gates>

<verification>
- `--auto-fit-script` flag, the AutoFitScript splice, and the entire BrowserFitJS surface (press/browserjs.go, themes.BrowserFitJS, themes/browser-fit.js) are removed; grep across *.go finds none of `BrowserFitJS`/`browser-fit.js`/`auto-fit-script`.
- Default HTML (htmlDocOptions{}) emits zero `<script>` (gate test); the InjectScripts SSE-reload seam for watch/serve is preserved.
- press/autofit.go marker emission unchanged; press/autofit_test.go green (markers inert on web, consumed by Flutter in 08-07).
- NOTICE drops the browser-fit.js clause (keeps Marp CSS attribution); AGENTS.md reflects zero-JS HTML + Flutter-only auto-fit; standing gates + addlicense green.
</verification>

<success_criteria>
- No viewer-side auto-fit JavaScript ships in any default HTML output path; the flag, splice, and vendored JS engine are gone; proven by a gate (criterion 4, web half).
- The `<!--fit-->`/shrink markers remain emitted (inert on web) for the Flutter consumer; NOTICE + AGENTS.md updated.
</success_criteria>

<output>
After completion, create `.planning/objectives/08-math-autofit/08-06-SUMMARY.md`.
</output>
