---
objective: 08-math-autofit
trd: "01"
type: standard
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - internal/latex2mathml/go.mod
  - internal/latex2mathml/go.sum
  - internal/latex2mathml/LICENSE
  - internal/latex2mathml/README.md
  - internal/latex2mathml/converter.go
  - internal/latex2mathml/walker.go
  - internal/latex2mathml/commands.go
  - internal/latex2mathml/tokenizer.go
  - internal/latex2mathml/symbols_parser.go
  - internal/latex2mathml/unimathsymbols.txt
  - NOTICE
  - .github/workflows/ci.yml
autonomous: true
requirements: []
must_haves:
  truths:
    - "`git.sr.ht/~mekyt/latex2mathml` is vendored VERBATIM into `internal/latex2mathml/` (its own go.mod module path preserved) and a `replace git.sr.ht/~mekyt/latex2mathml => ./internal/latex2mathml` directive in the root go.mod redirects the existing import path at the in-repo copy — so `press/math/mathml.go`'s `l2m \"git.sr.ht/~mekyt/latex2mathml\"` import site is BYTE-FOR-BYTE unchanged and the whole tree builds against the fork."
    - "The fork is BEHAVIOR-IDENTICAL to the external dependency at landing: `go build ./...`, `go vet ./...`, and `go test ./...` (including press/math's existing TestMathML/TestFallback/TestMathRender and the conformance gates) are all green with the vendored copy in place — this TRD adds the fork mechanism and NOTHING else; the five converter patches land in 08-02/08-03 against this in-repo copy."
    - "The vendored copy preserves latex2mathml's OWN MIT LICENSE (Copyright (c) 2023 mekyt) untouched, `internal/latex2mathml/**` is added to the addlicense `-ignore` set (it is third-party MIT, NOT AO-Cyber-stamped, matching the themes/** + convert/chrome/fonts/** verbatim-asset precedent), and `addlicense -check` stays green."
    - "The NOTICE `latex2mathml` entry's `[verify exact license in the math objective]` placeholder is REPLACED with the confirmed license (MIT, Copyright (c) 2023 mekyt, upstream git.sr.ht/~mekyt/latex2mathml @ v0.0.0-20231214134936-808832af73fc) plus the fork provenance: vendored in-repo at internal/latex2mathml/, patched by Objective 8 for the 5 converter root-causes, redistributed under its own MIT terms. The sibling go-latex/latex placeholder is resolved the same way (BSD-3-Clause — verify against its LICENSE)."
    - "No OTHER dependency churn: go.mod gains ONLY the `replace` directive (the fork mechanism); `go mod tidy` is never run; the vendored module's own dep (`github.com/neruyzo/etree`, already an indirect dep of the root module) resolves through the existing go.sum with no new require lines beyond the replace."
  artifacts:
    - internal/latex2mathml/converter.go
    - internal/latex2mathml/walker.go
    - go.mod
    - NOTICE
  key_links:
    - "The go.mod `replace` directive is the SOLE fork seam: because it redirects the existing import path (not a new one), every press/math import site and the frozen press.Options.MathMode contract are untouched, and 08-02/08-03 patch converter.go/walker.go IN internal/latex2mathml/ with zero import-path ripple. This TRD OWNS go.mod for Objective 8 — 08-02..08-07 never edit it."
    - "internal/latex2mathml/ is the in-repo converter every downstream math TRD (08-02 big-op/sqrt, 08-03 binom/pmatrix/aligned/mathvariant, and the 08-04 fallback-rule that assumes the aligned fix landed) patches or reasons against — vendored ONCE here, early, so the 2 fork-only bugs (\\sqrt[n], big-operator limits) become fixable at all."
---

<objective>
Vendor `git.sr.ht/~mekyt/latex2mathml` into the repository as an in-tree fork at `internal/latex2mathml/`, wired through a single go.mod `replace` directive so the existing `press/math` import path resolves to the in-repo copy with zero import-site churn. This is the FORK MECHANISM only — the prerequisite that makes the two structurally-unfixable-from-outside bugs (`\sqrt[n]` radicand loss, big-operator limit stacking) patchable at all (research Pitfall 2: "no fork exists yet ≠ no fork needed"). It lands the copy behavior-identical, preserves latex2mathml's MIT license/attribution, and resolves the NOTICE license placeholders the objective inherited.

Purpose: RESOLVED DECISION #1 is FORK + patch — fix all 5 converter root-causes in ONE coherent in-fork path. Doing the vendor + replace + licensing as its own foundation TRD keeps go.mod ownership isolated (03-01 precedent), so the parallel/sequenced patch TRDs (08-02, 08-03) only ever touch converter.go/walker.go/test files, never go.mod.
Output: `internal/latex2mathml/` (verbatim vendored module) + go.mod `replace` + NOTICE/license attribution + addlicense ignore — build/test green, behavior unchanged.
</objective>

<file_tree>
internal/                                   ← CREATE (in-repo fork root; NOT ./internal Go-visibility-only — this holds a full replace-target module)
└── latex2mathml/                           ← CREATE  (verbatim copy of the upstream module + LICENSE)
    ├── go.mod                              ← CREATE  (module git.sr.ht/~mekyt/latex2mathml — path PRESERVED so replace resolves)
    ├── go.sum                              ← CREATE
    ├── LICENSE                             ← CREATE  (mekyt's MIT — preserved verbatim, never AO-stamped)
    ├── README.md                          ← CREATE
    ├── converter.go                        ← CREATE  (patched in 08-02/08-03; verbatim here)
    ├── walker.go                           ← CREATE  (patched in 08-02; verbatim here)
    ├── commands.go                         ← CREATE
    ├── tokenizer.go                        ← CREATE
    ├── symbols_parser.go                   ← CREATE
    └── unimathsymbols.txt                  ← CREATE  (data file the parser reads — copy verbatim)
go.mod                                       ← MODIFY  (add the single `replace` directive — no other churn)
go.sum                                       ← MODIFY  (replace side-effect only)
NOTICE                                       ← MODIFY  (resolve latex2mathml + go-latex license placeholders + fork provenance)
.github/workflows/ci.yml                     ← MODIFY  (add `-ignore 'internal/latex2mathml/**'` to the addlicense invocation)
</file_tree>

<execution_context>
@~/.claude/devflow/workflows/execute-trd.md
@~/.claude/devflow/templates/summary.md
</execution_context>

<embedded_context>

<codebase_examples>
The upstream module is already in the local Go module cache — copy it verbatim from there (this is the exact pinned version go.mod requires today):

```
SRC=/Users/justin/go/pkg/mod/git.sr.ht/~mekyt/latex2mathml@v0.0.0-20231214134936-808832af73fc
# contents: commands.go converter.go walker.go tokenizer.go symbols_parser.go
#           unimathsymbols.txt go.mod go.sum LICENSE README.md example/
```

The upstream module identity to PRESERVE (its go.mod, so the replace target is a valid module):
```
module git.sr.ht/~mekyt/latex2mathml
go 1.21
require github.com/neruyzo/etree v0.0.0-20230816193247-70b7b06b18ad
```
`github.com/neruyzo/etree` is ALREADY an indirect dep of the root module (go.mod line 41) — no new require needed; it resolves through the existing root go.sum.

The ONLY import site in the whole repo (leave it EXACTLY as-is — the replace directive does the redirection):
```go
// press/math/mathml.go:26
l2m "git.sr.ht/~mekyt/latex2mathml"
// press/math/mathml.go:79
return l2m.Convert(raw, mathMLNamespace, display, 0)   // Convert(latex, xmlns, display, indent) string
```

The go.mod replace directive to add (bottom of go.mod, its own block):
```
replace git.sr.ht/~mekyt/latex2mathml => ./internal/latex2mathml
```

The addlicense invocation lives in `.github/workflows/ci.yml` (~line 70) — verbatim-asset dirs are already ignored; ADD `internal/latex2mathml/**`:
```
addlicense -l mit -s -c "AO Cyber Systems" \
  -ignore 'conformance/corpus/cases/**' -ignore '**/node_modules/**' \
  -ignore 'themes/**' -ignore 'convert/chrome/fonts/**' \
  -ignore 'internal/latex2mathml/**' \
  -check .
```

The NOTICE placeholder to resolve (NOTICE ~line 67):
```
latex2mathml (https://github.com/roniemartinez/latex2mathml)
License: [verify exact license in the math objective — out of scope for Objective 0]
```
The vendored LICENSE header confirms: `MIT License / Copyright (c) 2023 mekyt`. Note the upstream SOURCE Eden Press actually depends on is `git.sr.ht/~mekyt/latex2mathml` (a Go port), not the roniemartinez Python project the placeholder URL cites — correct the URL to the sourcehut module while resolving the license.
</codebase_examples>

<anti_patterns>
- Do NOT run `go mod tidy` — go.mod/go.sum are hand-authored, additive-only (CONTRIBUTING.md "Dependency management — additive-only, NEVER go mod tidy"). Add the replace directive by hand; `go build ./...` reconciles go.sum additively.
- Do NOT change the vendored module's own module path in internal/latex2mathml/go.mod — it MUST stay `git.sr.ht/~mekyt/latex2mathml` or the `replace` target won't match the import and the build breaks.
- Do NOT edit `press/math/mathml.go`'s import or Convert call — the whole point of the replace mechanism is that import sites are untouched (research: "keep the import sites in press/math unchanged").
- Do NOT AO-Cyber-stamp any vendored latex2mathml file — it is verbatim third-party MIT (Copyright mekyt). It goes in the addlicense `-ignore` set exactly like themes/** and convert/chrome/fonts/**; its own LICENSE file is the attribution.
- Do NOT patch any converter bug in THIS TRD — this is the fork mechanism only. `go test ./...` must prove behavior is UNCHANGED from the external dep. The 5 fixes are 08-02/08-03's job.
- Do NOT vendor the upstream `example/` dir if it pulls test-only deps that bloat go.sum — copy the library sources + LICENSE/README/go.mod/go.sum; drop `example/` if it introduces churn (note the omission in SUMMARY).
</anti_patterns>

<error_recovery>
- If `go build ./...` fails with "module git.sr.ht/~mekyt/latex2mathml@… found, but does not contain package" after adding the replace: the vendored go.mod is missing or its module path was altered — restore it to the verbatim upstream `module git.sr.ht/~mekyt/latex2mathml`.
- If `github.com/neruyzo/etree` shows as an unexpected new/updated require in the diff: the vendored go.sum diverged from root — the etree version is already pinned at go.mod:41; ensure the vendored copy's go.mod requires the SAME pseudo-version and do NOT bump it.
- If addlicense -check flags files under internal/latex2mathml/: the `-ignore 'internal/latex2mathml/**'` entry did not land in ci.yml (or a local run used the un-updated command) — add it; verbatim third-party assets are never stamped.
- If `go test ./...` shows a math behavior DIFF at this stage (before any patch): the copy is not verbatim (a file was truncated/re-saved with different line endings) — re-copy from the module cache byte-for-byte.
</error_recovery>

</embedded_context>

<context>
@.planning/PROJECT.md
@.planning/objectives/08-math-autofit/OBJECTIVE.md
@.planning/objectives/08-math-autofit/08-RESEARCH.md
@press/math/mathml.go
@go.mod
@NOTICE
</context>

<gotchas>
HARD constraints for this TRD (apply to every task):
- **NEVER port 8080** (permanently occupied). No port needed here.
- **go.mod is OWNED by THIS TRD for Objective 8.** Add ONLY the replace directive; NEVER `go mod tidy`; downstream TRDs (08-02..08-07) must not edit go.mod.
- **Commit ONLY via** `node ~/.claude/devflow/bin/df-tools.cjs commit "<msg>" --files <paths>` — NEVER raw `git commit` (a hook blocks it). Commit each task the MOMENT its gates are green.
- Every new AO-Cyber `.go` file carries the Eden MIT header ending in `// SPDX-License-Identifier: MIT`. The vendored latex2mathml files are the EXCEPTION — verbatim third-party, keep their original headers/absence, LICENSE file preserved, addlicense-ignored.
- Module path is `github.com/AO-Cyber-Systems/eden-press`, go 1.26.
- **Standing gates (every objective-8 TRD's `<verify>` runs all of these):** `gofmt -l`, `go build ./...`, `go vet ./...`, `go test ./...`, `go test ./conformance/...`, `bash scripts/check-no-chromedp.sh`, `bash scripts/check-cli-imports.sh`, `go test ./profiles/slides/ -run TestGrepGate` (Obj-2 grep-gate), `addlicense … -check .`.
</gotchas>

<tasks>

<task type="auto">
  <name>Task 1: Vendor latex2mathml verbatim into internal/latex2mathml/ + add the go.mod replace directive (behavior-identical)</name>
  <files>internal/latex2mathml/go.mod, internal/latex2mathml/go.sum, internal/latex2mathml/converter.go, internal/latex2mathml/walker.go, internal/latex2mathml/commands.go, internal/latex2mathml/tokenizer.go, internal/latex2mathml/symbols_parser.go, internal/latex2mathml/unimathsymbols.txt, internal/latex2mathml/LICENSE, internal/latex2mathml/README.md, go.mod, go.sum</files>
  <action>
Copy the pinned upstream module VERBATIM from the local module cache into `internal/latex2mathml/`, then redirect the import path with a replace directive:

```sh
SRC=/Users/justin/go/pkg/mod/git.sr.ht/~mekyt/latex2mathml@v0.0.0-20231214134936-808832af73fc
mkdir -p internal/latex2mathml
cp "$SRC"/{go.mod,go.sum,LICENSE,README.md,commands.go,converter.go,walker.go,tokenizer.go,symbols_parser.go,unimathsymbols.txt} internal/latex2mathml/
chmod -R u+w internal/latex2mathml   # module cache is read-only
```

Then append the replace directive to the ROOT go.mod (its own block, by hand — NOT go mod tidy):
```
replace git.sr.ht/~mekyt/latex2mathml => ./internal/latex2mathml
```

# CRITICAL: preserve internal/latex2mathml/go.mod's `module git.sr.ht/~mekyt/latex2mathml` line UNCHANGED — the replace target must be a module of that exact path.
# CRITICAL: do NOT touch press/math/mathml.go — the replace does the redirection; the import stays `l2m "git.sr.ht/~mekyt/latex2mathml"`.
# GOTCHA: github.com/neruyzo/etree is already pinned (root go.mod:41) — no new require; `go build ./...` reconciles go.sum additively. NEVER `go mod tidy`.
# GOTCHA: if example/ drags test-only deps, omit it (library sources + LICENSE/README/go.mod/go.sum suffice); note omission in SUMMARY.

Prove behavior is UNCHANGED (this TRD adds the fork, not any fix): the existing press/math tests (TestMathML asserts <msup>/<mfrac>; TestFallback; TestMathRender) and the conformance gates must pass exactly as before against the vendored copy.
  </action>
  <verify>gofmt -l internal/latex2mathml/*.go && go build ./... && go vet ./... && go test ./press/math/... ./... && go test ./conformance/... && bash scripts/check-no-chromedp.sh && bash scripts/check-cli-imports.sh && go test ./profiles/slides/ -run TestGrepGate && git diff --exit-code press/math/mathml.go</verify>
  <done>internal/latex2mathml/ holds the verbatim module; root go.mod has the single replace directive; `go build ./...` resolves the import to the in-repo copy; all existing tests + conformance + no-chromedp + cli-imports + Obj-2 grep-gate green; press/math/mathml.go is byte-for-byte unchanged.</done>
  <recovery>If build reports "does not contain package," the vendored go.mod's module path was altered — restore to `git.sr.ht/~mekyt/latex2mathml`. If a math test DIFFs, the copy is not verbatim — re-copy from the cache (see error_recovery).</recovery>
</task>

<task type="auto">
  <name>Task 2: Preserve latex2mathml license + addlicense-ignore the fork + resolve the NOTICE placeholders</name>
  <files>internal/latex2mathml/LICENSE, .github/workflows/ci.yml, NOTICE</files>
  <action>
Establish the vendored fork's licensing posture so `addlicense -check` stays green and attribution is correct:

1. Confirm `internal/latex2mathml/LICENSE` is the upstream MIT text VERBATIM (`Copyright (c) 2023 mekyt`) — preserved unmodified from the copy in Task 1.
2. Add `-ignore 'internal/latex2mathml/**'` to the addlicense invocation in `.github/workflows/ci.yml` (~line 70), alongside the existing `themes/**` and `convert/chrome/fonts/**` verbatim-asset ignores. The fork is third-party MIT (mekyt), NOT AO-Cyber-stamped.
3. Resolve the NOTICE `latex2mathml` placeholder (~line 67): replace `License: [verify exact license in the math objective — out of scope for Objective 0]` with the confirmed license + fork provenance:
   - License: MIT, Copyright (c) 2023 mekyt
   - Upstream: git.sr.ht/~mekyt/latex2mathml @ v0.0.0-20231214134936-808832af73fc (correct the URL from the roniemartinez Python project to the sourcehut Go module)
   - Provenance: vendored VERBATIM into internal/latex2mathml/ (module path preserved, redirected via a go.mod `replace`), PATCHED by Objective 8 for 5 converter root-causes (\\sqrt[n], big-operator limits, binom/pmatrix fence, aligned→mtable, mathvariant→codepoint); redistributed under its own MIT terms; MIT permits modification + redistribution.
4. Resolve the sibling `go-latex/latex` placeholder (~line 70) the same way: verify its LICENSE (codeberg.org/go-latex/latex — BSD-3-Clause) and record it; it is an ordinary go.mod dependency (NOT vendored), so no addlicense-ignore is needed for it.

# GOTCHA: mekyt's MIT header/absence stays as upstream ships it — do NOT prepend the AO-Cyber header block to any internal/latex2mathml file.
# PATTERN: mirror the NOTICE STIX Two Math entry's structure (source + tag + "bundled VERBATIM" + license) for the fork provenance wording.
  </action>
  <verify>addlicense -l mit -s -c "AO Cyber Systems" -ignore 'conformance/corpus/cases/**' -ignore '**/node_modules/**' -ignore 'themes/**' -ignore 'convert/chrome/fonts/**' -ignore 'internal/latex2mathml/**' -check . && grep -q "Copyright (c) 2023 mekyt" internal/latex2mathml/LICENSE && ! grep -q "verify exact license in the math objective" NOTICE</verify>
  <done>The vendored LICENSE is preserved verbatim; ci.yml's addlicense ignores internal/latex2mathml/**; `addlicense -check` is green; the NOTICE latex2mathml + go-latex placeholders are resolved with confirmed licenses and fork provenance.</done>
  <recovery>If addlicense -check still flags fork files, the -ignore glob did not match (check quoting: `'internal/latex2mathml/**'`). If NOTICE still has the placeholder string, the replacement missed a second occurrence — grep for "verify exact license" and resolve all.</recovery>
</task>

</tasks>

<validation_gates>
<build>go build ./...</build>
<vet>go vet ./...</vet>
<test>go test ./... && go test ./conformance/...</test>
<gates>bash scripts/check-no-chromedp.sh && bash scripts/check-cli-imports.sh && go test ./profiles/slides/ -run TestGrepGate</gates>
<license>addlicense -l mit -s -c "AO Cyber Systems" -ignore 'conformance/corpus/cases/**' -ignore '**/node_modules/**' -ignore 'themes/**' -ignore 'convert/chrome/fonts/**' -ignore 'internal/latex2mathml/**' -check .</license>
</validation_gates>

<verification>
- `go build ./...` resolves `git.sr.ht/~mekyt/latex2mathml` to `internal/latex2mathml/` via the replace directive; `git diff press/math/mathml.go` is empty (import sites untouched).
- Behavior unchanged: `go test ./...` (incl. press/math TestMathML/TestFallback/TestMathRender) and `go test ./conformance/...` green with the vendored copy — no math output DIFF vs the external dep.
- `bash scripts/check-no-chromedp.sh` and `bash scripts/check-cli-imports.sh` PASS (the fork adds no chromedp; internal/ is a pure-Go converter).
- `go test ./profiles/slides/ -run TestGrepGate` PASS (Obj-2 grep-gate — no regression).
- `addlicense … -ignore 'internal/latex2mathml/**' -check .` green; internal/latex2mathml/LICENSE preserves mekyt's MIT; NOTICE placeholders resolved.
- `go.mod` diff shows ONLY the added `replace` directive — no `go mod tidy`, no other require churn.
</verification>

<success_criteria>
- latex2mathml is an in-repo fork at internal/latex2mathml/, wired via a go.mod replace directive, with press/math import sites unchanged and all tests/conformance/gates green.
- The fork's MIT license/attribution is preserved and the NOTICE license placeholders (latex2mathml + go-latex) are resolved with fork provenance; addlicense -check green.
- The foundation is in place for 08-02/08-03 to patch converter.go/walker.go for the 5 root-causes without any go.mod or import-path churn.
</success_criteria>

<output>
After completion, create `.planning/objectives/08-math-autofit/08-01-SUMMARY.md`.
</output>
