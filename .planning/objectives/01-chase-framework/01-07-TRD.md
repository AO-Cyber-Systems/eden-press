---
objective: 01-chase-framework
trd: "07"
type: standard
wave: 4
depends_on: ["01-05", "01-06"]
files_modified:
  - chase/markdown/image.go
  - chase/markdown/background.go
  - chase/markdown/inlinesvg.go
  - chase/markdown/advancedbg.go
  - chase/markdown/markdown.go
  - chase/markdown/render.go
  - chase/markdown/background_test.go
autonomous: true
requirements: [PARSE-06, PARSE-07]
must_haves:
  truths:
    - "`![bg …](url)` alt-text options parse: bg, size keywords (fit = contain alias), split (left|right[:NN%]), vertical|horizontal, NN%, w:/h: with units, and filter functions with their documented default amounts"
    - "Inline-SVG mode wraps EVERY slide as `<svg data-marpit-svg viewBox=\"0 0 W H\"><foreignObject width=W height=H><section…>` where W/H come from the active theme (default 1280×720)"
    - "With inline-SVG DISABLED, a single bg image becomes a backgroundImage LOCAL directive (reuses 01-06's apply path) — no new HTML structure"
    - "With inline-SVG ENABLED, bg images trigger advanced-background mode: three foreignObjects per slide (background / content / pseudo) in that DOM order, with figures, split width/x adjustment, and `--marpit-advanced-background-split`"
    - "marp-bg-color, marp-bg-image, marp-bg-split render byte-for-byte matching the corpus (modulo Objective-3 batteries)"
  artifacts:
    - path: chase/markdown/image.go
      provides: "bg alt-text option-grammar parser (mirrors background_image/parse.js + image/parse.js) incl. filter defaults"
      contains: "func "
    - path: chase/markdown/background.go
      provides: "bg → backgroundImage local directive (non-SVG path) reusing 01-06 apply"
    - path: chase/markdown/inlinesvg.go
      provides: "Inline-SVG base transformer: wrap every section in <svg><foreignObject>; viewBox W/H from theme"
    - path: chase/markdown/advancedbg.go
      provides: "Advanced-background transformer: 3-layer (background/content/pseudo) structure + figures + split adjust"
    - path: chase/markdown/background_test.go
      provides: "Option-grammar + inline-SVG + advanced-bg structural tests vs corpus fixtures"
      contains: "func Test"
  key_links:
    - from: chase/markdown/inlinesvg.go
      to: chase/markdown/render.go
      via: "svg/foreignObject/section node kinds rendered with viewBox + width/height"
      pattern: "data-marpit-svg|foreignObject|viewBox"
    - from: chase/markdown/advancedbg.go
      to: chase/markdown/render.go
      via: "3-layer background/content/pseudo sections + figure nodes rendered in DOM order"
      pattern: "data-marpit-advanced-background"
    - from: chase/markdown/background.go
      to: chase/markdown/apply.go
      via: "non-SVG single bg reuses the backgroundImage directive materialization"
      pattern: "backgroundImage|apply"
---

<objective>
Implement background-image syntax (PARSE-06) and inline-SVG slide mode (PARSE-07) in `chase/markdown`: the `![bg …]` alt-text option grammar, the per-slide inline-SVG `<svg><foreignObject><section>` wrap, and the advanced-background 3-layer structure. These are the last of the Marpit mechanics and the blocking dependency for auto-fit/advanced backgrounds downstream.

Purpose: All 18 corpus HTML fixtures assume inline-SVG (every expected.html carries `data-marpit-svg`), so the inline-SVG wrap is required for ANY case to match byte-for-byte. Backgrounds have two modes: without inline-SVG a single bg is just a backgroundImage local directive; with inline-SVG (Marp Core's default) bg images become the 3-layer advanced-background SVG structure — reproduced byte-for-byte for marp-bg-image/marp-bg-split.

Output: `chase/markdown/{image.go, background.go, inlinesvg.go, advancedbg.go}` + modifications to markdown.go/render.go + tests turning the bg cases + inline-SVG structure green.
</objective>

<file_tree>
chase/markdown/
├── image.go             ← CREATE  (![bg …] alt-text option grammar + filter defaults)
├── background.go        ← CREATE  (non-SVG: bg → backgroundImage local directive)
├── inlinesvg.go         ← CREATE  (inline-SVG base wrap for every slide)
├── advancedbg.go        ← CREATE  (advanced-background 3-layer transformer)
├── markdown.go          ← MODIFY  (register inline-SVG + advanced-bg transformers, last)
├── render.go            ← MODIFY  (svg/foreignObject/figure/advanced-bg node kinds)
└── background_test.go   ← CREATE
</file_tree>

<execution_context>
@~/.claude/devflow/workflows/execute-trd.md
@~/.claude/devflow/templates/summary.md
</execution_context>

<embedded_context>

<codebase_examples>
Consume the Section nodes (01-05), the directive apply path (01-06 — non-SVG bg reuses backgroundImage materialization), and the theme size resolver (`chase/theme` ResolveSize → widthPixel/heightPixel). NOTE: `chase/markdown` must NOT import `chase/theme` at parse time (RESEARCH build order). Pass the resolved width/height into the inline-SVG transformer via an option / pc key (default 1280×720), not via a chase/theme import.

Every new .go file MUST begin with the Eden MIT header (verbatim from `conformance/runner/runner.go`). CI runs `addlicense -check`.
</codebase_examples>

<anti_patterns>
- DO NOT hardcode 1280×720 in the SVG wrap. Read the active theme's resolved dimensions (passed via option/pc). Default is 1280×720 only when nothing overrides it.
- DO NOT build a second directive system for bg. Non-SVG single bg REUSES the backgroundImage local-directive apply path from 01-06.
- DO NOT reorder the advanced-background layers. DOM order is strictly background → content → pseudo (RESEARCH §"Inline-SVG Mode").
- "fit" is an ALIAS for `contain`, not a separate CSS value.
</anti_patterns>

<error_recovery>
- The advanced-background static CSS (columns:initial, flex, figure styling, etc.) is injected by the THEME pipeline (TRD 01-04 pass_advancedbg.go), NOT here. This TRD emits the HTML structure; 01-04 emits the CSS. They meet at the corpus/cssdiff gate in 01-08.
- Split layout: the content foreignObject's `width = 100% - splitSize`, and for split=left `x = splitSize`; the background section carries `style="--marpit-advanced-background-split:{size};"`. Confirm against `marp-bg-split/expected.html` (in context) — it shows `width="50%" … x="50%"` and `--marpit-advanced-background-split:50%`.
- Multiple bg images stack per `direction` (horizontal|vertical); each becomes a `<figure style="background-image:url(&quot;…&quot;);">` (note the &quot;-escaped URL) with an optional `<figcaption>` when alt text is present.
</error_recovery>

</embedded_context>

<context>
@.planning/PROJECT.md
@.planning/objectives/01-chase-framework/01-RESEARCH.md
@conformance/corpus/cases/marp-bg-split/expected.html
@conformance/corpus/cases/marp-bg-image/input.md
@conformance/corpus/cases/marp-basic/expected.html
</context>

<research_context>
From 01-RESEARCH.md §"Background Images", §"Inline-SVG Mode":
- Full bg option grammar with regexes + filter default amounts (blur→10px, brightness→1.5, contrast→2, grayscale→1, hue-rotate→180deg, invert→1, opacity→.5, saturate→2, sepia→1, drop-shadow→'0 5px 10px rgba(0,0,0,.4)').
- Base inline-SVG wrap (every slide): `<svg data-marpit-svg="" viewBox="0 0 W H"><foreignObject width=W height=H><section…>…</section></foreignObject></svg>`; W/H from `themeSet.getThemeProp(theme,'widthPixel'|'heightPixel')`.
- Advanced-background: single `<svg>` gets THREE foreignObject children — (1) background layer `<section data-marpit-advanced-background="background">…<div data-marpit-advanced-background-container … direction>…<figure …>[<figcaption>]</figure>…</div></section>`, (2) content layer (split width/x adjust + `--marpit-advanced-background-split`), (3) pseudo layer (empty placeholder). Verbatim structure in RESEARCH; `marp-bg-split/expected.html` in context is the byte-for-byte target.
</research_context>

<gotchas>
- **NEVER port 8080** (occupied). Use 8091 only if a server is ever needed (not here).
- **go.mod is owned centrally. NEVER run `go mod tidy`** in this worktree. No new deps.
- **Commit ONLY via** `node ~/.claude/devflow/bin/df-tools.cjs commit "<msg>" --files <paths>` — NEVER raw `git commit` (hook blocks it).
- Wave-4 job: MODIFIES markdown.go + render.go (created by 01-05, modified by 01-06). Runs strictly after 01-06 — no parallel conflict. Keep edits additive: register the inline-SVG + advanced-bg transformers LAST (highest priority number, so they wrap after directives are applied); add new node-kind cases to the render switch without disturbing 01-05/01-06 paths.
- The inline-SVG wrap must run AFTER directive application (01-06) so the section already carries its data-*/style attrs before being wrapped.
</gotchas>

## Test list
(test_list_first required — write failing tests first; happy → edge → failure)

1. Option parse: `bg` → background flag; `bg fit` → size=contain (fit alias); `bg left:40%` → split left @40%; `bg vertical` → direction vertical; `bg 50%` → size 50%.
2. Option parse: `bg w:300px h:50%` → width/height with units; bare number → px.
3. Option parse: `bg blur` → filter blur(10px) default; `bg drop-shadow` → default '0 5px 10px rgba(0,0,0,.4)'.
4. Inline-SVG base wrap: `# A` (inline-SVG on) → `<svg data-marpit-svg="" viewBox="0 0 1280 720"><foreignObject width="1280" height="720"><section id="1">` (matches marp-basic structure sans heading slug).
5. viewBox uses theme-resolved W/H (e.g. 4:3 → 960×720) when the size is overridden.
6. Non-SVG single bg → backgroundImage local directive on the section; no advanced-bg structure.
7. Advanced-bg (inline-SVG on): `![bg left](url)` + `# Split` → 3 foreignObjects (background/content/pseudo) with figure, split width=50%/x=50%, `--marpit-advanced-background-split:50%` — matches marp-bg-split/expected.html.
8. marp-bg-color, marp-bg-image render byte-for-byte (modulo Objective-3 batteries).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ![bg …] option-grammar parser</name>
  <files>chase/markdown/image.go, chase/markdown/background_test.go</files>
  <action>
Port `background_image/parse.js` + `image/parse.js` alt-text option grammar into a parser over an image node's alt text:
- keywords: bg, size (auto|contain|cover|fit — fit aliases contain), split (regex `^(left|right)(?::((?:\d*\.)?\d+%))?$`), direction (vertical|horizontal), NN% resize, w:/width: + h:/height: with unit set {%,ch,cm,em,ex,in,mm,pc,pt,px; bare→px}.
- filters: blur/brightness/contrast/grayscale/hue-rotate/invert/opacity/saturate/sepia/drop-shadow, each with the documented default amount when omitted (see RESEARCH values).
Return a structured `BgOptions` (background bool, size, split side+pct, direction, width/height, filters).
Write Test-list cases 1, 2, 3 as failing tests first.
  </action>
  <verify>go test ./chase/markdown/ -run 'Bg|Option|Image|Filter' passes; `go vet ./chase/markdown/`</verify>
  <done>The bg option grammar parses all size/split/direction/dimension/filter forms with correct defaults; fit aliases contain.</done>
  <recovery>Diff a failing option against the exact regex/branch in background_image/parse.js and port it verbatim.</recovery>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inline-SVG base wrap + non-SVG bg path</name>
  <files>chase/markdown/inlinesvg.go, chase/markdown/background.go, chase/markdown/markdown.go, chase/markdown/render.go, chase/markdown/background_test.go</files>
  <action>
1. inlinesvg.go — an ASTTransformer (registered LAST, after directive apply) that, when inline-SVG mode is enabled, wraps every Section in svg/foreignObject nodes. Add `*Svg`/`*ForeignObject` node kinds; render.go emits `<svg data-marpit-svg="" viewBox="0 0 W H"><foreignObject width="W" height="H">…</foreignObject></svg>`. W/H come from the resolved theme size (option/pc, default 1280×720) — do NOT import chase/theme.
2. background.go — non-SVG mode: a single bg image becomes a backgroundImage LOCAL directive on its slide, reusing 01-06's apply path (no new structure). Hide the bg image from normal flow.
3. markdown.go — register the inline-SVG transformer last; gate advanced-bg (Task 3) behind inline-SVG enabled.
Write Test-list cases 4, 5, 6 as failing tests first.
# GOTCHA: inline-SVG wrap runs AFTER 01-06 so sections already carry directive attrs.
  </action>
  <verify>go test ./chase/markdown/ -run 'Svg|Inline|NonSvg|Background' passes; a plain slide renders the exact marp-basic svg/foreignObject structure (sans slug).</verify>
  <done>Every slide wraps in `<svg data-marpit-svg viewBox><foreignObject W H><section…>`; viewBox honors theme size; non-SVG single bg reuses the backgroundImage directive.</done>
  <recovery>If the svg attributes/order differ, compare against marp-basic/expected.html (in context) exactly.</recovery>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Advanced-background 3-layer transformer</name>
  <files>chase/markdown/advancedbg.go, chase/markdown/render.go, chase/markdown/background_test.go</files>
  <action>
When inline-SVG enabled AND a slide has bg image(s), produce the 3 foreignObject layers per slide (DOM order background → content → pseudo):
1. background: `<foreignObject W H><section data-marpit-advanced-background="background" [split attrs] style="--marpit-advanced-background-split:{size};"><div data-marpit-advanced-background-container="true" data-marpit-advanced-background-direction="{horizontal|vertical}"><figure style="background-image:url(&quot;{url}&quot;);…">[<figcaption>{alt}</figcaption>]</figure>…</div></section></foreignObject>` (repeat figure per image).
2. content: original section, `data-marpit-advanced-background="content"`; for split, foreignObject `width=100%-split`, and split=left → `x=split`; section carries `--marpit-advanced-background-split`.
3. pseudo: `<foreignObject data-marpit-advanced-background="pseudo" W H><section data-marpit-advanced-background="pseudo" …style="[color if set]"></section></foreignObject>` (empty).
Add the node kinds + render cases. Register the transformer after inline-SVG wrap.
Write Test-list cases 7, 8 as failing tests first, targeting marp-bg-split + marp-bg-image byte-for-byte.
# CRITICAL: the advanced-bg CSS is emitted by chase/theme (01-04); this TRD emits only the HTML structure.
  </action>
  <verify>go test ./chase/markdown/ passes fully; the rendered HTML for marp-bg-split equals `marp-bg-split/expected.html` (via htmldiff), modulo Objective-3 batteries.</verify>
  <done>Advanced-background produces the exact 3-layer structure with figures, split width/x adjustment, and the split custom property; marp-bg-* cases match byte-for-byte structurally.</done>
  <recovery>If a layer/attribute differs, compare against marp-bg-split/expected.html (in context) node-by-node; the &quot;-escaped URL and the empty pseudo section are easy to miss.</recovery>
</task>

</tasks>

<validation_gates>
<lint>gofmt -l chase/markdown/*.go | (! grep .) ; go vet ./chase/markdown/</lint>
<test>go test ./chase/markdown/</test>
<build>go build ./...</build>
</validation_gates>

<verification>
- `go test ./chase/markdown/` green; bg option grammar, inline-SVG base wrap, and advanced-background 3-layer all pass.
- Rendered HTML for marp-basic/marp-bg-color/marp-bg-image/marp-bg-split matches the corpus (via htmldiff) modulo Objective-3 batteries (heading slugs, etc.).
- `chase/markdown` still does not import `chase/theme` (`go list -deps ./chase/markdown | grep -c 'chase/theme'` == 0).
- `addlicense … -check .` passes for the new files.
</verification>

<success_criteria>
PARSE-06 (background-image `![bg …]` syntax → CSS backgrounds or the advanced-background SVG layer) and PARSE-07 (inline-SVG slide mode `<svg><foreignObject><section>`) are implemented and pass byte-for-byte structural checks against the corpus bg cases.
</success_criteria>

<output>
After completion, create `.planning/objectives/01-chase-framework/01-07-SUMMARY.md`.
</output>
